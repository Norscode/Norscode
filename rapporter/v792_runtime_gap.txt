Norscode v792 runtime-gap diagnose

OK:
- Web/admin DEV virker
- /status.json virker
- /dev-login virker
- /logout virker
- Norscode check virker

MANGLER I STAGE0 RUNTIME:
- builtin.exec_prosess
- builtin.random_hex
- builtin.tid / now / timestamp

KONSEKVENS:
- Produksjon må fortsatt være blokkert
- Admin kan kun være DEV-only
- Ekte login må vente

NESTE VERSJON:
v793 bør legge inn tydelig runtime-statusside:
- /runtime-gap
- /status.json viser manglende builtins
