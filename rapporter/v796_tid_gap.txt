Norscode v796 tid-gap

Testet:
- builtin.tid()
- builtin.now()
- builtin.timestamp()

Resultat:
- Alle mangler i stage0 runtime.

Beslutning:
- Første offisielle navn bør være builtin.tid()
- builtin.now() og builtin.timestamp() kan bli alias senere.

Neste:
v797 finn hvor builtin-funksjoner registreres i runtime.
