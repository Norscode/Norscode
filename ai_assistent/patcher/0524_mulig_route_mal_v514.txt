# Norscode Web mulig route-mal v514

Mulige mønstre som må sjekkes i eksisterende app:

1. funksjon get_om(ctx) -> tekst
2. funksjon route_om(ctx) -> tekst
3. funksjon handter(ctx) -> tekst
4. funksjon app(ctx) -> tekst
5. route metadata i compiler/NCB
6. web.request_context fra std.web

Neste steg:
Finn faktisk mønster i tools/macos-window-host/app.no.
