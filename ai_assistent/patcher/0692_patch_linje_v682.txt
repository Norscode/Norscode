# Patch linje v682

Gammel:
la hv = builtin.trim(builtin.slice(line, c + 1, lengde(line)))

Ny:
la hv = builtin.trim(builtin.slice(line, c + 1, lengde(line) + 1))

Fiks for headerverdi som mister siste tegn.
