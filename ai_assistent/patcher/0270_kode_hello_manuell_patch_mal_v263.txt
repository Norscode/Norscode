# Norscode AI kode_hello manuell patch-mal v263

Dette er bare en MANUELL patch-mal.
Ikke bruk automatisk.

Trygg metode senere:
1. Backup:
   cp ai.no ai.no.bak_kode_hello_rydding_v263

2. Legg eventuelt inn import hvis nødvendig:
   bruk ai_moduler.kodegen_hello_v261 som kodehello

3. Endre bare kroppen til kode_hello() slik at den bruker modulen:
   funksjon kode_hello() -> tekst {
       returner kodehello.kode_hello()
   }

4. Test:
   nc check ai.no
   printf "kod hello.no = hello" > spm.txt
   nc run ai.no

5. Hvis feil:
   cp ai.no.bak_kode_hello_rydding_v263 ai.no
   nc check ai.no

Regel:
Ikke slett funksjonen ennå. Første steg er bare å delegere.
