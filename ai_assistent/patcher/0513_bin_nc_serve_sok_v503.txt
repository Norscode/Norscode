# Norscode bin/nc serve søk v503

Mål:
- Finne om nc allerede har kommando som ligner serve/server/http.

Kommandoer:
grep -R "serve" -n prosjekter/Norscode/bin prosjekter/Norscode/tools 2>/dev/null
grep -R "server" -n prosjekter/Norscode/bin prosjekter/Norscode/tools 2>/dev/null
grep -R "http" -n prosjekter/Norscode/bin prosjekter/Norscode/tools 2>/dev/null

Regel:
Dette er bare lesing/kartlegging.
Ikke endre bin/nc.
