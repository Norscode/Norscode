# Norscode Web serve route funn v521

Funn fra grep:
- serve_runner leser ncb["route_handlers"].
- route_handlers blir laget av web.route("GET /sti").
- tools/macos-window-host/app.no bruker dette mønsteret:
  funksjon hjem(ctx: ordbok_tekst) -> ordbok_tekst {
      web.route("GET /")
      ...
  }

Konklusjon:
NorscodeWeb trenger en serve-kompatibel app med web.route().
