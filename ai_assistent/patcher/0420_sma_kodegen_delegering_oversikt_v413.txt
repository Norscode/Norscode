# Norscode AI små kodegen delegeringsoversikt v413

Forventede delegeringer i ai.no:

returner kodehello.kode_hello()
returner kodefil.kode_fil_leser()
returner kodekalk.kode_kalkulator()
returner kodefunk.kode_funksjon_demo()
returner kodeminne.kode_minne_demo()
returner kodeai.kode_ai_demo()
returner kodestd.kode_standard_app()

Kontroll:
grep -n 'kodehello.kode_hello' ai.no
grep -n 'kodefil.kode_fil_leser' ai.no
grep -n 'kodekalk.kode_kalkulator' ai.no
grep -n 'kodefunk.kode_funksjon_demo' ai.no
grep -n 'kodeminne.kode_minne_demo' ai.no
grep -n 'kodeai.kode_ai_demo' ai.no
grep -n 'kodestd.kode_standard_app' ai.no
