# Deploy til norscode.no plan v428

Mål:
- Kunne kjøre NorscodeWeb på server.
- .no-filer ligger som app-kode.
- public/ serveres statisk.
- TLS/HTTPS håndteres av reverse proxy eller innebygget server senere.

Mulig produksjonsflyt:
Internet -> HTTPS -> reverse proxy -> Norscode server -> .no sidefil -> HTML

Start trygt lokalt før produksjon.
