# Beslutning v594

Hvis Set-Cookie vises i curl:
- serve_runner støtter cookie-header
- neste fase kan lage trygg session-cookie-demo

Hvis Set-Cookie ikke vises:
- vi må undersøke serve_runner header-normalisering
- ikke bygg ekte login før cookie virker
