# Beslutning v614

Hvis cookie finnes i ctx["headers"]["cookie"]:
- bygg cookie-parser i neste fase

Hvis cookie finnes i ctx["cookie"] eller ctx["cookies"]:
- bruk det enkleste feltet

Hvis ingen cookie er synlig:
- sjekk serve_runner ctx-bygging og header normalisering
