# Norscode AI kode_standard_app manuell patch v383

Dette er bare en MANUELL patch-mal.
Ikke bruk automatisk.

Senere manuell metode:

1. Backup:
   cp ai.no ai.no.bak_kode_standard_app_v383

2. Legg eventuelt inn import:
   bruk ai_moduler.kodegen_standard_app_v381 som kodestd

3. Endre bare kroppen til kode_standard_app():

funksjon kode_standard_app() -> tekst {
    returner kodestd.kode_standard_app()
}

4. Test:
   nc check ai.no
   printf "kod app.no = app" > spm.txt
   nc run ai.no

5. Hvis feil:
   cp ai.no.bak_kode_standard_app_v383 ai.no
   nc check ai.no
