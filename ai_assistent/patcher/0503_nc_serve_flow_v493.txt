# Norscode Web nc serve flow v493

Foreslått intern flyt:

HTTP request:
GET /om HTTP/1.1

nc serve:
1. parse method = GET
2. parse path = /om
3. response = app.handter("/om")
4. send HTTP headers/body

Norscode app:
app.no -> app_router_v471 -> render/static -> response
