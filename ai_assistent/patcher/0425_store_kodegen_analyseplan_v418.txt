# Norscode AI store kodegen analyseplan v418

Neste fase bør kartlegge store kodegen-funksjoner:

grep -n 'funksjon kode_nettsidebygger' ai.no
grep -n 'funksjon kode_index_html' ai.no
grep -n 'funksjon kode_style_css' ai.no
grep -n 'funksjon kode_norscode_nettside' ai.no
grep -n 'funksjon lag_kode_for_tema' ai.no
grep -n 'funksjon lag_kodefil' ai.no

Regel:
Bare kartlegging først.
Ingen sletting.
Ingen automatisk endring.
