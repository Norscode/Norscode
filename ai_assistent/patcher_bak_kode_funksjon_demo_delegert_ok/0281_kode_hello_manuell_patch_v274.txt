# Norscode AI kode_hello manuell patch v274

Dette er manuell patch.

Gammel funksjon:

funksjon kode_hello() -> tekst {
    returner "funksjon start() -> heiltall {\n" +
        "    skriv(\"Hei frå Norscode!\\n\")\n" +
        "    returner 0\n" +
        "}\n"
}

Ny delegert funksjon:

funksjon kode_hello() -> tekst {
    returner kodehello.kode_hello()
}

Viktig:
- Ikke slett funksjonsnavnet.
- Ikke endre lag_kode_for_tema ennå.
- Test umiddelbart etter endring.
