# Norscode AI kodegen grep-kommandoer v252

Kjør disse kommandoene:

grep -n '^funksjon kode_' ai.no
grep -n '^funksjon lag_kode' ai.no
grep -n 'kode_nettsidebygger' ai.no
grep -n 'kode_standard_app' ai.no
grep -n 'kode_ai_demo' ai.no
grep -n 'kode_minne_demo' ai.no

Lagre gjerne resultatet:
grep -n '^funksjon kode_' ai.no > prosjekter/Norscode/ai_assistent/patcher/kodegen_funksjoner_funnet.txt
grep -n '^funksjon lag_kode' ai.no >> prosjekter/Norscode/ai_assistent/patcher/kodegen_funksjoner_funnet.txt
