# Plan med avhuking: 100 % Norscode-eid verktøykjede

Denne fila er den sannferdige arbeidsloggen for sjølvstendigheitsløypa.
Ei oppgåve blir berre avhuka når ho er implementert og verifisert av den
levande porten. Kandidatfiler er ikkje det same som promotert stage0.

**Avhukingsstatus 2026-08-02: 57 av 76 punkt er verifiserte, 19 står opne.**

## Mål og reglar

- [ ] Aktiv utvikling, kompilering, testing, bygging, pakking og CI blir eigd av Norscode `.no`.
- [ ] Shell, Python, C og Zig er fjerna frå aktiv verktøykjede.
- [x] OS-systemkall og signerings-API er avgrensa til smale plattform-ABI-ar.
- [x] `dist/norscode_native` og committed stage0 blir ikkje endra før full kandidatverifisering.
- [x] Dokumentasjon kan ikkje markere ein funksjon ferdig før den levande porten er grøn.

## Fase 1 – Sannferdige portar og baseline

- [x] Opprett isolert arbeidsgren/worktree utan å røre eksisterande lokale endringar.
- [x] Registrer komplett baseline for L5, L5b, full test og release-preflight.
- [x] Legg til `./bin/nc selvstendighet --strict`.
- [x] La strict-porten køyre aktiv-flate, bootstrap, L5, L5b, full test, slow-lane, seed og single-binary.
- [x] Behald kort normalport utan å hevde at han dekkjer full testflate.
- [x] Gjer `local-green --strict` og CI avhengige av strict-porten.

## Fase 2 – Reparer L5b

- [x] Reproduser stackfeilen med precompiled cache på og av.
- [x] Instrumenter VM-en med funksjon, opcode, instruction pointer, operandstack, rammedjupn og siste trygge punkt.
- [x] Fullfør eigne permanente stackbalanseportar for bundler, dynamiske kall, `INDEX_GET`, GC-røter og VM-i-VM-kall.
- [x] Reparer runtime-/codegen-feilen utan å auke stackgrensa.
- [x] Regenerer precompiled kandidatar først etter grøn kjeldebane.
- [x] Køyr L5b-mini tre gonger med cache på og av.
- [x] Køyr full L5b tre gonger med cache på og av.
- [x] Stadfest byte-identisk kandidat A/Gen2 og grøn, byte-identisk L5 Gen1/Gen2.

### Fase 2-evidens

- Kandidat v93: den underliggjande cachefeilen var den dynamiske VM-brua si legacy-serialisering, som tolka tekstoperandane `"null"`, `"0"`, `"true"` og JSON-liknande tekst som andre typar. `selfhost.vm` brukar no typebevarande `json_skriv`; cachemarkøren lever berre over VM/AOT-grensa og blir fjerna før kanonisk bundle-output.
- Gjeldande L5 er byte-identisk Gen1/Gen2 på 1 716 097 byte. Full L5b er 3/3 grøn med cache på (1 716 097 byte) og 3/3 grøn source-only med cache av (1 662 962 byte). L5b-mini er 3/3 grøn i begge modus.
- Den samla kandidat-strict-porten er grøn på 3 554,78 s med 4 687 577 088 byte maksimal RSS: bootstrap A+B/C, L5, L5b, shellfri seed, single-binary, 779 normaltestar og 13 slow-lane-testar, 0 feil og 24 klassifiserte normalhopp av 803 testtilfelle.
- Seed-porten bruker no committed stage0 i eit isolert tempområde med `CC=__clang_not_allowed__` og `PATH=/nonexistent`; han skriv aldri til `dist` eller committed stage0.
- Modulglobal-kandidat v89: L5 Gen1/Gen2 er byte-identisk etter kanonisk NCB-serialisering (1 713 410 byte). Full L5b er 3/3 grøn utan cache (1 677 401 byte) og 3/3 grøn med regenerert cache (1 713 410 byte). L5b-mini er 3/3 grøn i begge modus med identiske 721-byte-bundlar og hash `9f04bab6234d4223e793780af19f96d0316690ecf46a541a23ce8a7b6ae669e8`.
- Full L5b og L5b-mini brukar no strukturert `norscode-process-spawn-v1`; dei aktive eigarane inneheld ikkje `exec_prosess`, shellkommando eller `sh -c`.

- L5 etter kandidatcache-reparasjon: 1 244 775 byte identisk Gen1/Gen2.
- Full L5b cache av: 3/3 grøne, 1 642 484 byte identisk.
- Full L5b cache på: 3/3 grøne, 1 244 764 byte identisk.
- Cache av: 167,68 s / 430 882 816 byte RSS; 167,51 s / 431 079 424 byte RSS; 167,40 s / 430 489 600 byte RSS.
- Cache på: 37,71 s / 269 598 720 byte RSS; 37,75 s / 270 761 984 byte RSS; 37,58 s / 269 795 328 byte RSS.
- Ekstra sluttkontroll på gjeldande kode: cache av 1 642 490 byte identisk (166,72 s), cache på 1 244 775 byte identisk (37,93 s).
- Mini etter kandidatcache-reparasjon: 3/3 cache av og 3/3 cache på, identiske 799-byte-bundlar; 6,13 s og 5,97 s i første målte runde.
- Strict-port på gjeldande kode: grøn på 2 453,37 s med 1 435 811 840 byte maksimal RSS.
- Normal testflate i strict: 722 bestått, 0 feil, 18 klassifiserte hopp.
- Slow-lane i strict: 8 bestått, 0 feil, 732 klassifiserte lane-hopp.
- Release-preflight-baseline: 20,61 s / 316 342 272 byte RSS, éin klassifisert feil på Windows stage0/kandidat-hash.
- `local-green --strict`: stoppar korrekt i steg 1 på same release-preflight-sperre.
- Windows-hashar: stage0 `4e30aef18e037e2eff20b7307908cc5328e7842e6e1faa6a1340bae48a9b4bed`, kandidat `c36abc7e5477082fdd07acc597a8927b302466062cad65d1f9edc1269881f3b1`.
- Tillitsankera `dist/norscode_native` og `bootstrap/stage0/norscode-macos-arm64` er ikkje promoterte.
- Permanent stackmatrise: bundler-normalisering, 600 dynamiske `ncb_call_fn`-kall, native `INDEX_GET`-matrise, GC-root/minor/major/compaction og 64 gjentekne kall gjennom ei ekte indre Norscode-VM er grøne. `tests/test_stack_balance_gate_contract.no` låser delportane.
- Plattform-only-kandidat v127 avdekte at cachematerialisatoren brukte prefiksmatching, slik at `std.runtime.process_abi` feilaktig vart gruppert som `std.runtime` og transitive bundlermodular kom i feil byte-rekkjefølgje. Materialisatoren vel no eksakt `module`-felt og bevarer Gen1-rekkjefølgja. Gjeldande L5 er 1 947 287 byte identisk Gen1/Gen2; full L5b er 1 947 287 byte identisk med cache på og 1 699 464 byte identisk mellom separat source-only kandidat A/B med cache av. L5b-mini, cachekontrakt og GC-rotdeduplisering er grøne.

## Fase 3 – Fjern shellavhengigheiter

- [x] Klassifiser dei aktive `.sh`-filene som wrapper, test, bygg, release eller plattform.
- [x] Fullfør Norscode-API for kataloglisting, kopiering, flytting, sletting, filmodus og mellombelse område.
- [x] Fullfør strukturert prosesskøyring med executable, argv, cwd, miljø, stdin, timeout og outputgrenser.
- [x] Implementer Norscode-eigd TAR, gzip, hashing og binær filflyt.
- [x] Erstatt shellkommandoar i standardbiblioteket før verktøy og CI blir migrerte.
- [x] Forby `sh -c`, `rm -rf`, `find`, `gzip` og `tar` i aktiv kjerneflate.
- [x] Erstatt `bin/nc`-shellscriptet med native dispatch gjennom `selfhost/nc_main.no`.
- [x] Arkiver eller slett alle erstatta aktive shellwrapperar.
- [x] La active-surface feile på kvar ny aktiv `.sh`-fil.
- [x] Flytt aktive CI-`run:`-blokker som orkestrerer `mkdir`, `cp`, `chmod`, `rm`, løkker, C/Zig-kompilering, Docker eller shellskript til Norscode-eigde `.no`-jobbar; berre direkte oppstart av Norscode og smale plattform-/signeringsgrenser kan stå att.

### Fase 3-evidens

- Første levande inventar 2026-07-30 var 115 shellfiler, ikkje det eldre estimatet på 112.
- Ferskt filinventar etter wrappermigrering: 0 aktive `.sh`-filer i wrapper-, test-, bygg-, release- eller plattformkategorien. Dette var åleine ikkje fullført fase 3; den innebygde workflow-shellflata måtte òg migrerast.
- Den levande Norscode-eigde strict-porten `tools/ci_shell_surface_inventory.no` finn 119 aktive CI-`run:`-blokker: alle 119 er direkte Norscode-bootstrap og 0 inneheld shellorkestrering. Stage0-/artefaktmaterialisering, fixturekopiering, testmiljø, notariseringslisting, hashing/pakking, Windows bygg/attestasjon, native GC-overgang, pakkeinstallasjon og Pebble/Docker-orkestrering har `.no`-eigarar med strukturert argv. `tools/ci_runtime_fileops.no` bruker native filmodus når ABI-en finst og ein eksplisitt strukturert `/bin/chmod`-plattformreserve for eldre committed stage0, etterfølgt av selftest; dette er ein smal, versjonert plattformgrense og ikkje shelltekst. Den maskinlesbare rapporten ligg i `reports/ci-shell-surface-inventory.json`; `active-surface` låser baselinen til 0, og `NORSCODE_CI_SHELL_STRICT=1` er grøn.
- Alle 115 baselinerte shellfiler er fjerna frå aktiv flate og bevarte under `archive/legacy_shell/` der historikk framleis er relevant.
- Maskinlesbar rapport: `reports/shell-surface-inventory.json`, generert av `tools/shell_surface_inventory.no` via native filesystem-ABI.
- `bin/nc` har no 0 utførande kall til aktive `.sh`-verktøy; dei tre attverande `.sh`-treffa er berre filnamn i release-sporingslista.
- `bin/nc` er no den native Mach-O-dispatchen (lenkje til den promoterte `dist/norscode_native`), medan det gamle shellscriptet ligg under `archive/legacy_shell/bin/nc`. Kandidat v143 og aktiv dist er hash-identiske med SHA-256 `5f2a626ae1859df81c1adac97aacce3427841a6702c279c471ec1bab8c6c323a`; committed stage0 er med vilje uendra på `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`.
- Den levande native CLI-porten kontrollerer Mach-O/ELF/PE-signatur, byte-/hash-identitet mot dist og Norscode-eigd argv-/verktøydispatch. Dei fem migrerte CLI-/releasekontrakttestane er grøne, og `active-surface`, fase 0, L5 og L5b med cache på og av er grøne etter promoteringa.
- Full `release-preflight` er framleis raud med to sannferdige feil utanfor fase 3: Windows stage0/cross-kandidat-avviket i fase 5 og ein utdatert cache-kontrakt i fase 6.
- Hybridkompilering, local-green, L5b, sjølvstende, seed, omgang6/6b, stage0-assets, feature-gate, scaffold, bootstrap-regenerering og REPL-uttrykk går no direkte til `.no`-eigarane utan shellreserveveg.
- `nc repl '1 + 2'` er levande verifisert til `=> 3` gjennom `norscode-process-spawn-v1`.
- Ein separat, ikkje-promotert macOS-kandidat er bygd frå runtime-kjelda og har levande verifisert `readline`, `chmod`, fil-unlink og tom katalogsletting. Interaktiv `nc repl` gav `=> 3` og avslutta via stdin-linje-ABI-en.
- `std/tempfile.no` er migrert bort frå `mktemp`, `mkdir`, `rm` og `exec_prosess`; levande test dekkjer unik fil/katalog, les/skriv, filsletting og rekursiv tømming.
- `std.runtime_filesystem` har Norscode-eigde API-ar for mkdir, streaming-kopiering, flytting og rotbundet tempområde. Levande management-test er grøn.
- `std/pathlib.no` er migrert bort frå `pwd`, `find`, `test`, `rmdir`, `rm`, `mv` og `exec_prosess`; levande test dekkjer fil/katalogtype, listing, glob, storleik, binær flytting og sletting.
- Release-policyen krev no fråvær av dei fjerna shellreservevegane. Levande `release-preflight` er tilbake til éin feil, berre Windows stage0/cross-kandidat-avviket.
- POSIX-kandidaten bruker `unlinkat(..., AT_REMOVEDIR)` og `fchmod` på rotbundne handtak. Windows-backenden bruker verifiserte `CreateFileW`-handtak og kompilerer grønt for x86-64 med Zig cross-frontend.
- `std/fileinput.no` er migrert frå `cp` til binær Norscode-les/skriv; både kjeldeport og levande inplace-backup-test er grøne.
- `std/getpass.no` er migrert frå `bash`, `stty`, `test -t` og `whoami` til `norscode-terminal-v1`. Kandidatens pipe-test stadfestar at skjult input utan TTY feilar lukka og aldri fell tilbake til synleg passordinput.
- `std/linecache.no` bruker no rein Norscode SHA-256 over filbytes i staden for ekstern `stat`; levande test oppdagar ei innhaldsendring med same teiknlengd og ugyldiggjer cachen.
- `std/locale.no` les `LC_ALL`, `LC_CTYPE` og `LANG` gjennom miljø-ABI-en og formaterer lokalt utan `locale`-prosessen; levande norsk miljøtest er grøn.
- `norscode-system-info-v1` leverer OS, node, versjon, arkitektur, CPU-tal, totalminne, arbeidskatalog og køyrbar sti på macOS/Linux/Windows. macOS-kandidaten og Windows cross-kompilering er grøne.
- `std.platform` og `std.sys` er migrerte frå `uname`, `nproc`, `sysctl`, `free`, `whoami`, `lsb_release` og `which` til systeminfo-ABI-en.
- `std.prosess` køyrer synkron kommandotekst som strukturert executable/argv via `norscode-process-spawn-v1`, avviser shellmetateikn og gjer PATH-oppslag gjennom ein `process.exec`-beskytta `norscode-system-v1`-operasjon.
- Den strukturerte prosessplanen fører no eksplisitt miljøkart gjennom både synkron og asynkron ABI. Levande kandidatprøve køyrer `/usr/bin/env` med lukka arva miljø og verifiserer den innsette verdien; eksisterande portar dekkjer cwd, stdin, timeout, outputgrense og async-handle.
- Den gamle spool-/bakgrunnsshell-bana i `std.prosess` er fjerna. Kompatibilitets-API-et for async bruker no det same generasjonsbundne native handtaket for poll, streaming, signal, timeout og close; native async-port og adapterport er grøne.
- Filesystem-ABI-en har no rotbunden `path_stat` med type, modus, inode, lenkjetal, eigar, storleik og tidsfelt på POSIX og Windows.
- `std.stat` er migrert frå `stat`, `test`, `chmod` og `chown`-prosessar til path-stat/filmodus-ABI; eigarskifte feilar lukka fram til ein privilegert ABI finst. Levande kandidatport fann og retta dei gamle logiske `og`-maskene og verifiserer no modus, type og storleik.
- `std.md5` implementerer RFC 1321 i rein Norscode og består fem kjende testvektorar.
- `std.filecmp` er migrert frå `diff`, `md5sum`, `md5`, `find` og `stat` til Norscode-MD5, filesystem-ABI og intern linjediff. Levande kandidatprøve dekkjer like/ulike filer, kataloglisting og eksakt filhash.
- `std.wsgiref` brukar no berre Norscode HTTP/TLS og Norscode-filsletting; curl-, kill- og rm-reservevegane er fjerna, og framande PID-ar utan native handle blir avviste fail-closed.
- `std.syslog` brukar `norscode-system-log-v1`: POSIX `syslog(3)` og Windows Event Log ligg bak ABI-en, medan ein avgrensa runtime-ring gir prosessfri lesing/søk. Levande macOS-kandidatprøve er grøn.
- `std.plistlib` les og skriv XML i Norscode og brukar `norscode-property-list-v1` for binær plist via CoreFoundation. Levande kandidatprøve er grøn for XML, binær rundtur og automatisk `bplist`-deteksjon utan `plutil`.
- `std.protocol_stream` gir avgrensa, shell-fri tekstprotokoll over native TCP/TLS. `std.poplib` og `std.imaplib` brukar denne for både klartekst og TLS, utan curl, og feilar lukka ved manglande serversvar.
- `std.webbrowser` bruker strukturert executable/argv-policy og Norscode-eigd PATH-oppslag utan `sh -c`, `which` eller legacy `exec_prosess`; ugyldige URL-skjema og ukjende nettlesarar er levande verifiserte fail-closed.
- `std.smtplib` brukar no den same native protokollstraumen for SMTP og implicit TLS utan curl eller sendmail. Meldings-, header- og adressevalidering er levande verifisert; STARTTLS feilar eksplisitt lukka fram til same-forbindelse TLS-oppgradering finst.
- `std.filops` er migrert til Norscode-filsystemet og den versjonerte filesystem-ABI-en. Den ikkje-promoterte kandidaten består levande binær kopi, metadata, katalogtre, flytting, listing, globbing, stinormalisering, tempfil og rekursiv sletting.
- `std.shutil` deler no den prosessfrie `std.filops`-kjernen. Kandidatprøva dekkjer kopi, katalogtre, flytting, listing, tømming og sletting; arkivformat utan Norscode-backend feilar førebels eksplisitt lukka.
- `std.gzip` brukar no Norscode-eigd binær RFC 1952-flyt med stored DEFLATE-blokker, CRC32 og ISIZE-kontroll. Levande kandidatprøve dekkjer vilkårlege byte, tekst/base64 og filobjekt; fixed/dynamic Huffman blir framleis avvist fail-closed.
- `std.tarfile` brukar no den pure TAR-kjernen for USTAR-header, header-checksum, binær payload, listing, gzip, medlemsutpakking og traversal-sikker heilutpakking. Den direkte host-porten er grøn; den portable VM-i-VM-bana fullfører testen først når ho blir avbroten og er registrert som ein reell VM-i-VM-/ytingfeil.
- `std.shutil` tilbyr no `tar` og `gztar` gjennom desse Norscode-kjernane; zip, bzip2 og xz feilar framleis lukka.
- `std.ftplib` har persistent native kontrollkanal og EPSV/PASV-datakanal for listing, binær nedlasting og opplasting. Kjelda er prosessfri og levande injeksjons-/fail-closed-prøve er grøn; full interoperabilitetsprøve mot ein attestert FTP-server står att.
- Standardbibliotekinventaret er redusert frå 23 til 0 modular med shell-/eksternprosessrestar (to av dei fjerna treffa var berre stale kommentar/generatortekst).
- `selfhost.vm` opprettar katalogar gjennom filesystem-bindinga og bruker monotont millisekundstempel utan `mkdir`/`date`-prosess. Mach-O/ELF-codegen set køyrbar modus gjennom filesystem-ABI-en utan `chmod`.
- L5-kompilerer kvar modul i ein isolert Norscode-prosess via `norscode-process-spawn-v1`, med eksplisitt miljøkart og utan shell eller `bin/nc` som mellomledd. Levande L5 etter siste bundlerendring er grøn med 1 243 916 byte identisk Gen1/Gen2.
- `active-surface` listar no repositoryet gjennom den rotbundne, suffiksfiltrerte filesystem-ABI-en utan `find`/shell. Alle 115 baselinerte wrapperane er fjerna frå aktiv flate, og porten avviser kvar ny aktiv `.sh`.
- Linux AppDir-eigarfilen er migrert frå `rm`/`mkdir`/`cp`/`chmod`/`tar` og eksterne SHA-verktøy til `std.filops`, `std.shutil`, filesystem-ABI og rein Norscode SHA-256. Ein AppDir er bygd levande med kandidaten, shell-fallbacken i `bin/nc` er fjerna, og `tools/package-linux-app.sh` er flytta til `archive/legacy_shell/tools/`.
- Random-/systemkjeldeinventaret går no rekursivt gjennom filesystem-ABI-en utan `find`, shell eller ekstern prosess. Den levande kandidaten fann kjelder i alle konfigurerte tre, og `tools/check_random_sources_v694.sh` er arkivert.
- `random_hex`-runtimeproben bruker den same native katalogtraverseringa og avgrensar søket til aktiv kjeldeflate i staden for å gå gjennom byggartefaktar. Levande probe fann kjende `random_hex`-kall, og `tools/probe_random_hex_runtime_v712.sh` er arkivert.
- Legacy-CVM/NCBB-gaten traverserer no `tools/` gjennom filesystem-ABI-en, les treffkandidatar i Norscode og startar ingen `find`- eller shellprosess. Den levande kandidaten er grøn, og `tools/no_legacy_cvm.sh` er arkivert; CLI sporar berre arkivkopien for releasehistorikk.
- Python-avhengnadsrapporten traverserer repoet gjennom filesystem-ABI-en, filtrerer `.py` i Norscode og lagar `reports/` gjennom native katalog-ABI. Levande audit er grøn med 0 Python-filer i `tools/`, og `tools/python_dependency_audit.sh` er arkivert.
- AST-/IR-kanonisering, backend-rekkefølgjerapport, semantisk diff og symboltabellserialisering er levande køyrde som prosessfrie `.no`-verktøy. To skjulte `fil_finst`-restar vart retta til den faktiske filesystem-bindinga, og alle fem shellinngangane er arkiverte.
- Surface-ownership-gaten traverserer no heile den aktive flata gjennom `norscode-native-filesystem-v1`, utan `find`, shelltekst eller ekstern prosess. Den levande gaten, kjeldekontrakten, CLI-kontrakten og release-kontrakten er grøne; shell-reserven er arkivert.
- Filesystem-ABI-en har no absolutt, komponentverifisert rot, tom rot-relativ vandring, suffiksfilter og eksplisitte prune-prefiks. Symbolske lenker blir aldri følgde. Levande prøve dekkjer relativ rot, absolutt rot og filtrert repository-vandring.
- Kandidat `build/norscode_native_native_dispatch_candidate_v13`, hash `4dc1e1cea12825857951d15512f32150c5719b01691ef19abf9692775eed0fb2`, køyrer `doctor` direkte gjennom `selfhost/nc_main.no` og avviser overskytande argument med status 2. Den sentrale native tabellen eig no 14 nye argumentfrie bygg-, bootstrap- og CI-kommandoar; argumentkrevjande shellkontraktar står framleis opne.
- På same v13-hash er full direkte fase0 grøn på 36,00 s / 146 554 880 byte RSS etter cachebom og atomisk cachepublisering. `active-surface` er grøn på 18,54 s / 111 132 672 byte RSS; den gamle VM-vandringa vart først avbroten etter 179,55 s utan delresultat.
- Kandidat `build/norscode_native_native_dispatch_candidate_v14`, hash `8e42c43e936de3946db145d81efa89afcd7f3205fe9e9c187948833c8b8a24b6`, eig no argumentkontraktane for release-preflight, releasepakking/-installasjon, local-green, bootstrap-regenerering, sjølvstende, Omgang6, stage0-assets/-seed og NCB-til-ELF. Levande OS-argv-prøver stadfestar hjelp med status 0, manglande obligatoriske argument med status 2, `maintenance --help` og prosessfri `local-green --list`.
- v14-bygginga avdekte og låste den manglande AOT-mappinga for `builtin.system_info`; generatoren deklarerer no den smale plattform-ABI-en før genererte kall. Kjeldekontrakten og den komplette native linken er grøne. Generert C/wrapper vart fjerna etter bygging og er ikkje del av aktiv flate.
- Historisk v14-status: direkte fase0 var grøn på 35,16 s / 126 615 552 byte maksimal RSS og `active-surface` grøn på 16,50 s / 77 250 560 byte maksimal RSS. Shellinngangen som då stod att, er no erstatta av den promoterte v143-dispatchen dokumentert ovanfor.
- Active-surface eig no ein levande shelltekstport for `std/`, `selfhost/`, `compiler/` og aktive kjerneverktøy. Omgang6b, hybridbundle, pure-VM-gap, kontrollert promotering, macOS app/window/pakking og random-probene brukar native filoperasjonar i staden for `rm -rf`/`find`; macOS filikon brukar strukturert executable/argv i staden for `sh -c`. Direkte v14-port er grøn på 42,18 s / 111 706 112 byte RSS. Den eksplisitte Linux Docker-stage0-overgangslana er eigd av fase 5 og er ikkje rekna som kjerneflate.
- Native kandidat v15, hash `c3c458457d5749858b85f3ad9f93e828cb708efe7dac11b5b6a78face50b8f51`, legg til validerte OS-argv-kontraktar for Linux-pakking, Docker/Linux-stage0-overgang, Windows-pakking/layout og macOS-appverifikasjon. Hjelp og ugyldige argument er levande verifiserte med status 0/2, og ein importert argumenttest stadfestar alle miljøverdiar og rot-relative stiar utan å starte pakkebygg. På same hash er fase0 grøn på 38,43 s / 130 859 008 byte RSS og active-surface grøn på 41,51 s / 106 987 520 byte RSS. Full `bin/nc`-paritet står framleis att.
- Kandidat v16 utvida native argv-dispatch med macOS app-/windowbygg, pakking, signering og notarisering, samt Linux/macOS-installasjon. Kandidat v17 la til REPL, frasegenerering, feature-check, startproject/scaffold, startapp, migrate og test-hjelp. Dei levande hjelp-/feilprøvene returnerer status 0/2 som kontrakten krev.
- v17 avdekte at `builtin.system_info` og `builtin.system_operation` fanst i AOT-generatoren, men mangla i VM-i-VM-dispatchen. Begge er no eksplisitt vidaresende i `selfhost.vm`; `system_operation` krev `process.exec`. Host-bundlebyggjaren støttar versjonerte NCB-overlays slik at ei oppdatert VM-modul faktisk erstattar den innebygde funksjonen.
- Kandidat v19 bind direkte `test`-dispatch til eiga køyrbar fil gjennom `NC_NATIVE`; han fell ikkje lenger skjult tilbake til committed `dist`. Enkeltfiltestar hoppar over den kostbare heiltre-hashen med mindre `NC_TEST_CACHE=1`, medan fullflata framleis brukar innhaldsadressert cache.
- Gjeldande ikkje-promoterte native-dispatchkandidat er v23, hash `7e08a464522e95d04f133a3705697872337a813c301065f02740d657eae908f6`. CLI-dispatch-, argument-/miljø-, AOT/VM-builtin-, testpolicy- og profilerkontraktane er grøne mot kandidaten. Direkte argv set absolutt repositoryrot frå `norscode-system-info-v1`, slik at capability-scope og relative allowlistar er samde. Normal verktøy-VM kan velje eksplisitt NCB-bunden fastmodus; testmål blir framleis køyrde med `NORSCODE_VM_FAST=0` og profilerprøva med eksplisitt intervall er grøn.
- På v23 er direkte fase0 grøn på 12,83 s / 40 976 384 byte maksimal RSS, og active-surface er grøn på 13,65 s / 36 634 624 byte maksimal RSS. Full native `test <fil>`-paritet er likevel ikkje godkjend: den siste v22-målinga av ei lita direkte test var framleis ikkje ferdig etter 7 minutt og vart avbroten. Profilen viser at profileringskostnaden er fjerna, men `vm_heap_track`/VM-i-VM-kompilering er no dominerande. `bin/nc`-erstatningspunktet står derfor sannferdig ope.
- Kandidat v24, hash `489f0cd527c6ce39b68b215d869de76834d90a01a54340118f92641574f3d16b`, vidaresender SHA-256-builtinane gjennom selfhost-VM-en og køyrer små direkte enkeltfiltestar på om lag 8–9 sekund i staden for meir enn 7 minutt. Signatur-, testpolicy-, profiler- og prosess-scope-prøvene er grøne. Den store argument-/importprøva trefte først den eksplisitte 4 GiB-grensa og er ikkje godkjend; ho står som eit reelt modul-/minnegap.
- Historisk v33-status: kandidaten hadde hash `911f929593216984e06afcd691f7eca1e3dd8e0067b0cc700d5a0f3bb88100b9` og etablerte direkte kommando- og bundleparitet. Den då opne shellinngangen er no lukka gjennom verifisert v143-promotering.
- Kandidat v34, `build/norscode_native_full_host_current_cli_embedded_candidate_v34`, har hash `c2bb7c6d1d5250b0fc17edb0016e1bc4f5ad8755bcb5c8a30a4f5ed2fe502397` og embedda verifisert L5-kompilator og VM. Normal sjølvstendeport, bootstrap A/B/C, L5 (1 679 811 byte identisk), L5b-mini, cache-på L5b (1 679 811 byte identisk), seed og single-binary er grøne. Den komplette strict-porten 1. august 2026 er derimot raud i fulltesten: 28 feil fordelt på manglande Argon2id/ACME-backend, parallelle timeoutar og kontraktar som framleis peikar på arkiverte shellfiler eller gamle markørar. Kjøringa brukte 3 892,06 sekund og nådde 3 768 123 392 byte maksimal RSS. Kandidaten er derfor ikkje godkjend for å erstatte `bin/nc`, og avhukinga står med vilje open.
- Tillitsankera er framleis urørte: `dist/norscode_native` og `bootstrap/stage0/norscode-macos-arm64` har begge SHA-256 `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`.
- Bootstrap-gap-sjekklista og NCB-aliasnormalisering er levande køyrde direkte som `.no`. Normaliseringa skreiv ein separat kandidat utan å røre runtime, stage0 eller dist; begge shellinngangane er arkiverte og kontraktane er grøne.
- Runtime-basics, IR-/bytekodefase, codegen-diagnose og reelle pipeline-prober er levande køyrde frå `.no`-eigarane; dei fire aktive shellwrapperane er arkiverte. Runtime-basics og IR-/bytekodeporten er grøne på både aktiv macOS-dist og Linux x86-64 stage0 i Docker.
- IR-porten avdekte at den prioriterte `build/v9400`-hybridbundlen var eldre enn dei verifiserte precompiled-modulane: tom `returner` vart feilaktig slått saman med neste linje. AST-en og L5-kompilatoren var korrekte. Hybridbundlen er regenerert frå aktuell lexer, parser, semantikk og IR; direkte compile/runtime, reproduksjonstesten og releasekontrakten er grøne utan endring av `dist` eller stage0.
- Native-first-gaten og den allereie avvikla cookie-hotfixmeldinga er levande verifiserte direkte frå `.no`; begge shellwrapperane er arkiverte. CLI-kontrakten, releasekontrakten og active-surface er grøne etter flyttinga.
- Runtime-fase 3 køyrer no Linux runtime-gap og klientproben direkte med Linux stage0 i staden for å starte macOS-`bin/nc` inne i Docker. macOS- og Linux-runtime-gap er grøne; begge gamle stage0-binærane manglar framleis socket-backenden og blir rapporterte som `socket_status=pending`. `.no`-eigarporten returnerer korrekt grønt for dei fullførte runtime-gap-probene, shellwrapperen er arkivert, og release-/CLI-kontraktane er grøne.
- `active-surface` går direkte til `.no`-eigaren; den overflødige `tools/no_c_python_active_surface.sh` er arkivert. Ferskt inventar og levande port er grøne med 94 aktive, eigde shellfiler.
- `bin/nc repl` går direkte til `tools/nc_repl.no`; `tools/nc_repl.sh` er arkivert og overflata er grøn med 93 aktive, eigde shellfiler. Direkte uttrykksmodus gir `1 + 2 => 3`. Interaktiv modus mot committed stage0 feilar framleis eksplisitt på manglande `builtin.readline`; han er berre grøn på den ikkje-promoterte ABI-kandidaten og blir ikkje rekna som ferdig stage0-funksjon.
- `bin/nc test` går direkte til `tools/nc_test.no`; den historiske reserveimplementasjonen er flytta til `archive/legacy_shell/tools/nc_test.sh`. Native-first-gaten krev no den aktive `.no`-eigaren og fråvær av aktiv test-wrapper. Levande eigar-, policy-, CLI-, release- og native-first-portar er grøne; inventaret er 92 av 92 eigde shellfiler.
- Utviklaroppsettet går levande direkte gjennom `scripts/dev-setup.no`; med materialisert dist rapporterer det klart checkout-resultat og neste CLI-steg. Den reine launcher-wrapperen er arkivert, releaseporten krev fråvær på aktiv sti, og inventaret er 91 av 91 eigde shellfiler.
- `nc local-green` eig no heile aktive kommandoen gjennom `tools/local_green.no`; historisk reserveflyt er arkivert. Hjelp, normal liste og streng liste er levande grøne utan wrapper, og releaseporten krev at den aktive shellfila manglar. Inventaret er 90 av 90 eigde shellfiler.
- Python-fri CI har berre `tools/python_free_ci.no` i aktiv flate; shell-launcheren er arkivert og fråværet er låst i releaseporten. Eigarkontrakten peikar framleis på dei levande `active-surface`, seed- og testportane; den interne kommandoteksten står att i den separate shelltekst-migreringa. Inventaret er 89 av 89 eigde shellfiler.
- Vedlikehaldsdocs-policyen køyrer levande direkte i `tools/selfhost_maintenance_verify.no` og er grøn. Den fullstendige shell-reserven er arkivert og den aktive stien er låst tom.
- Fase-0-porten traverserer no flata gjennom `std.filops` og kallar eigarskapsgatane direkte som Norscode-modul, utan `sh -n`, `find` eller ekstern prosess. Direkte port, CLI-, kjelde- og releasekontraktar er grøne; wrapperen er arkivert. Inventaret er 87 av 87 eigde shellfiler.
- `cleanup_repo.no` og `repository_cleanup.no` deler ein ny shell-fri Norscode-kjerne med eksplisitt repository-markør, blokkering av rot/heimekatalog og vern av `.git`. Lett og full opprydding er levande grøn i isolert `/private/tmp` mot kandidaten. `std.filops` bruker no `/` som ABI-rot for absolutte katalogstiar. Wrapperane står att fordi committed stage0 enno ikkje kan utføre `rmdir`; dei blir ikkje arkiverte før den attesterte kandidaten er promotert.
- `startapp` og `startproject` bruker no prosessfrie Norscode-malmotorar med native listing, katalogoppretting, tekstsubstitusjon, binær kopiering og filmodus. Full app- og prosjektvariant er levande verifisert i isolert `/private/tmp`; prosjektet materialiserer `std`, `selfhost`, `runtime` og `compiler` utan `find`, `ln` eller `chmod`-prosess. Porten les dei faktiske maltestane i staden for gamle wrapperkommentarar. Begge wrapperane er arkiverte, og inventaret er 85 av 85 eigde shellfiler.
- Dei siste to aktive wrapperane, `cleanup_repo.sh` og `repository_cleanup.sh`, er arkiverte etter grøn kandidat-strict. Ferskt inventar er 83 aktive shellfiler med kategoriar test/bygg/release/plattform, `wrapper=0` og 83 av 83 `.no`-eigarar. Active-surface, CLI-kontrakt og releasekontrakt er grøne etter flyttinga.
- Den kjeldebygde kompilatoren avdekte og retta to nye determinismefeil: eksplisitt avsluttande `RETURN` får ikkje lenger eit ekstra implisitt null-return, og heksadesimale heiltal blir normaliserte med eksplisitt sifferverdi i staden for den feilkompilerte `char_code`-aritmetikken. Språktesten låser `0x00`, små/store A–F og `0xFF`; Mach-O- og Linux-ELF-portane er grøne med korrekt magic.
- Fersk L5 etter desse compilerrettingane er byte-identisk på 1 675 049 byte. L5b er byte-identisk med cache på 1 675 049 byte og med cache av 1 620 582 byte; source-only-runden materialiserte både generasjon A og B frå aktiv kandidat.
- Testløparen byggjer no cachekandidatar i shard-eigde tempområde og publiserer dei atomisk; felles `.tmp`-namn og lik millisekund-run-id kan ikkje lenger kollidere mellom åtte shards. Native trådattestasjon har ei eksplisitt 30-sekunders testgrense under kald åtte-shard-belastning utan å endre runtime-API-et.
- Endeleg normalflate på den korrigerte compilerkjelda: 748 bestått, 0 feil og 18 klassifiserte hopp av 766 testtilfelle. Endeleg slow-lane på same kjelde: 8 bestått, 0 feil og 758 eksplisitt lane-filtrerte av 766, med 600-sekunders grenser.
- Post-migrering normalflate på den frosne full-host-kjelda: fire deterministiske shards gav 747 bestått, 0 feil og 18 klassifiserte hopp av 765 testtilfelle. Den serielle kontrollen passerte over 800 testutfall utan feilmarkør, men vart avslutta av vertens prosessgrense med exit 143 før oppsummering og er derfor ikkje brukt som grønt bevis.
- Post-migrering slow-lane på same kjelde: 8 bestått, 0 feil og 757 eksplisitt lane-filtrerte testtilfelle av 765 totalt, med `TEST_TIMEOUT=600` og `SLOW_TEST_TIMEOUT=600`.
- Kandidat-runtime eksponerer OS-argument gjennom `norscode-argv-v1` i alle tre VM-dispatchbanane. Ei levande kandidatprøve mottok `norscode-argv-probe` som `argv[1]`; ein separat komplett host-kandidat køyrer no `--help`, `selftest`, `run` og `compile -o` direkte gjennom den genererte `selfhost.nc_main`-flata utan `NORSCODE_CMD`. Norscode-eigde verktøy byggjer host-bundlen og splicar den verifiserte argv-dispatchen utan å endre stage0. Resten av `bin/nc`-kommandoane står att før shellscriptet kan erstattast.
- Ny argv-kandidat `e703f71f688e8beeb1864fcc9ce49b4bcb58aa195b7cb482db125efe65c0a711` (5 474 056 byte) set eksplisitt target-capability/disk-policy for direkte `nc run`; miljøinput når dermed den indre VM-en. `--help`, `selftest` og den vesle L5b-fixturen er grøne. Komplekse kjeldeprogram avslører framleis `Stack underflow` i den eldre embedda host-kompilatoren (`kanoniske_linjer ip=4`, `stabil_hash ip=14`), så native CLI-erstatning er med vilje ikkje avhuka.
- Ein full-host-kandidat med 740 funksjonar blir no generert frå verifisert L5-kompilator, `nc_main` og den manglande semantic-validation-modulen. Første C-kompilering avslørte at stream-generatoren kalla generiske builtins med direkte signatur; signaturfeilane er retta og låste med test. Linken er no redusert til åtte konkrete manglande alias/host-bridge-symbol, mellom anna `kompiler_fil_aktiv`, `kompiler_objekt_aktiv` og tre semantic-alias. Full-host-kandidaten blir ikkje rekna som bygd før desse er løyste og den levande komplekse `run`-prøva er grøn.
- Full-host-linken er no løyst: builtin-signaturar går gjennom korrekt aliasbane, aktiv kompilering peikar på den bundla L5-kompilatoren, semantic-aliasa er eksplisitte, og `selfhost.ast` er med i host-bundlen. Kandidaten har 765 funksjonar, hash `79615fbce0216c5193d7c59bf7b1dcfc9eba4f62083cec6270ddb1e050224045` og storleik 5 305 816 byte. AST, IR, backend-rekkefølgje, semantisk diff og symbolserialisering køyrer direkte gjennom kandidatens OS-argv utan stack-underflow.
- Første embedded full-host-kandidat avslørte to ekte gatehol: NCB-image-traileren mangla, og legacy-linken var bygd utan dei sikkerheitsbackendane som aktiv macOS-runtime brukar. Ein ny kandidat fekk compiler- og VM-NCB som versjonert text-image og blei linka med den same statiske OpenSSL-adapteren som aktiv runtime. Argon2id, ACME-signering, ACME-verifisering og DNS-sonevalidering er grøne mot denne kandidaten; OpenSSL er framleis ein eksplisitt fase-7-adapter og ikkje rekna som fjerna.
- Gjeldande ikkje-promoterte full-host-kandidat er `build/norscode_native_full_host_crypto_embedded_candidate`: 12 514 962 byte, hash `c18953d445b7278316f73056c4ed86b6576915e276879d8183505bdd4cd5dff6`. På akkurat denne hashen er runtime-gap, bootstrap A+B, bootstrap C, L5 (1 243 916 byte identisk), cache-på L5b (1 243 916 byte identisk), single-binary utan repo/NCB/PATH og full testflate grøne.
- Kandidatens fulle normaltest på åtte deterministiske shards: 747 bestått, 0 feil og 18 klassifiserte hopp av 765 testtilfelle. Første kaldløp materialiserte kandidatbundne NCB-cacheoppføringar og synte eit stort fase-6-ytelsesgap, men endra ikkje testutvalet.
- Kandidatens slow-lane på åtte deterministiske shards: 8 bestått, 0 feil og 757 eksplisitt lane-filtrerte av 765 testtilfelle med 600-sekunders testgrense.
- Ny runtime-kandidat `build/norscode_native_full_host_current_runtime_embedded_candidate_v2` har hash `addc9b487c31849267ecd7b58f48fd74076347527257cec9152a66338b482d84` og er 13 256 002 byte. POSIX async-prosess-ABI-en fører no eksplisitt miljøkart heilt fram til `execv`; levande lukka-miljøtest og parallelle cleanup/scaffold-testar er grøne.
- Hybridkompilatoren beheld importalias og normaliserer testlokale modular til repo-relative modulnamn. Lokal `tests/butikk.no` og repo-importar frå `examples`, `tools` og `compiler` er grøne saman på fire shards.
- Endeleg normalflate etter import-, cache- og trådtimeoutreparasjon: åtte deterministiske shards, 747 bestått, 0 feil og 24 eksplisitte lane/native-hopp. Varm port tok 159,15 s og 1 353 252 864 byte maksimal RSS.
- Endeleg slow-lane på same kjelde og kandidat: 13 bestått, 0 feil og 757 eksplisitte lane/native-hopp. Porten tok 465,43 s og 1 618 427 904 byte maksimal RSS.
- Kald referanse før siste lane-reparasjon tok 3 104,97 s og 6 282 608 640 byte maksimal RSS. To AOT-prober feila berre under denne samtidige ressursbelastninga, bestod isolert og er no obligatoriske i slow-lane. Den siste varme normalporten er om lag 19,5 gonger raskare enn denne kalde testporten; dette er separat testportbevis. Det eigne 2× varm-bygg-målet er verifisert av dei fail-closed kompilatorrapportane i fase 6.
- Innhaldscachen er ikkje lenger bunden til runtimefilas sti, mtime eller storleik. Test-NCB-ar og modulfragment bruker eksplisitte bytekode-/compiler-/runtime-ABI-versjonar og SHA-256 over relevante kjeldebytes. Prosesseigde tempfiler gjer parallell publisering kollisjonsfri.
- `tools/nc_test_parallel.no` startar åtte strukturerte native prosessar med stabil sortering og indeks-modulo-sharding, utan `sh -c` eller shellbasert bakgrunnskøyring. Normal- og slow-portane er begge grøne gjennom denne eigaren.
- Tillitsankera er verifiserte urørte etter kandidat- og testarbeidet: `dist/norscode_native` og `bootstrap/stage0/norscode-macos-arm64` har begge hash `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`.
- Komplett `selvstendighet --strict` mot den ikkje-promoterte kandidaten er grøn på 2 441,82 s og 1 353 302 016 byte maksimal RSS. Porten beviste bootstrap A/B/C, L5, L5b, seed, single-binary, 747 normaltestar og 13 slow-testar. Endeleg standardkommando står framleis open til kandidaten kan promoterast lovleg.
- Python-auditen hoppar no over genererte `build/`, `.norscode/` og release-cacheområde og samlar berre `.py`-stiar under traverseringa. Strict-feilen på 6,60 GB RSS er reparert; levande audit er grøn på 4,39 s og 134 758 400 byte RSS.
- Committed stage0 er framleis urørt og avviser dei nye operasjonane fail-closed; den nye ABI-en blir ikkje promotert før den fulle kandidatporten i fase 4 er grøn.
- `norscode-native-filesystem-v1` har no rotbunden, atomisk `symlink_replace`: mål blir normalisert under capability-rota, vanlege filer/katalogar kan ikkje overskrivast, og publisering skjer med `symlinkat` + `renameat`. Levande kandidatprøve dekkjer oppretting, utskifting, trygg `../` og rotflukt-avvising; Windows feilar eksplisitt lukka til reparse-ABI-en er attestert.
- Rein Norscode USTAR bevarer no filmodus gjennom skriving, gzip, lesing og utpakking. Levande rundtur stadfestar `0755` for `AppRun`; `std.filops` slettar også broken symlinks gjennom rotbunden `unlinkat`.
- Linux AppDir-installasjonen brukar no berre Norscode SHA-256, gzip/USTAR, filoperasjonar, tempområde og atomiske lenker. Ei levande isolert installasjonsprøve bygde arkivet, verifiserte hash, installerte versjonen, las gjennom `current` og launcher og rydda alt utan ekstern prosess. `tools/install-linux-appdir.sh` er arkivert.
- Linux desktop-entry-installasjonen brukar no Norscode katalog-/kopieringsflyt og deterministisk validering av `Type`, `Name`, `Exec` og `Icon`, utan `pwd`, `mkdir`, `cp` eller desktop-prosessar. Levande isolert installasjon av desktopfil og binært ikon er grøn; wrapperen er arkivert.
- macOS app-installasjonen validerer `Info.plist` gjennom Norscode/CoreFoundation-ABI, bevarer bundlemodus med `std.filops` og publiserer både versjon og appnamn med atomiske native lenker. Levande isolert `1.2.3`-installasjon er grøn utan `PlistBuddy`, `plutil`, `cp`, `rm` eller `ln`; filikon er ei eksplisitt etteroperasjon og wrapperen er arkivert.
- Windows app-layout blir no bygd av ein importerbar Norscode-kjerne med native katalog-/kopieringsflyt og validerte appnamn. Ei binær fixture gav identiske `norscode.exe`/`nc.exe`-kopiar og gyldige CMD/PowerShell-launcherar utan `rm`, `mkdir`, `cp` eller ekstern prosess; dette er layoutbevis, ikkje ekte Windows-attestasjon.
- `std.zip` skriv no deterministiske stored-ZIP-ar med CRC32, lokale headrar, central directory og EOCD, og les dei tilbake med CRC-kontroll i Norscode. Windows-pakking kallar layoutkjernen direkte, sorterer filer stabilt, skriv ZIP/SHA-256 og ryddar staging utan `zip`, `shasum`, `mktemp`, `rm` eller `bin/nc` som underprosess; levande pakkerundtur er grøn.
- Kompilatorsnapshot og snapshot-samanlikning traverserer no filer med `std.filops`, sorterer stabilt og hash-ar binærinnhald med rein Norscode SHA-256. Ei levande prøve dekkjer identiske, ulike og einsidig manglande binærfiler; begge shellwrapperane er arkiverte.
- Bootstrap-artefaktpipen kopierer no filer og katalogtre, lagar stabilt inventar og hash-ar binærinnhald utan `mkdir`, `find`, `cp`, `date`, `shasum` eller ekstern prosess. Ei levande prøve dekkjer rapportfil, nøsta binærartefakt og manifest; wrapperen er arkivert.
- Stage0-manifestet listar sjølv `norscode-*`-artefaktar, sorterer filnamna og reknar rein Norscode SHA-256. Shell-input med ferdige hashar er fjerna, og levande prøve verifiserer binærhash, relativ sti, sortering og filtrering utan å skrive til committed stage0.
- Embed-generatoren les no NCB som binærbytes direkte og skriv C-arrayet utan `pwd` eller ekstern prosess. Levande prøve bevarer `00 ff 10 41`, output peikar på `.no`-eigaren, og shellwrapperen er arkivert; C-outputen er framleis ein eksplisitt overgangsartefakt fram til fase 5 er fullført.
- Compiler-samanlikning og bootstrap-diff hash-ar no binærbyte med rein Norscode SHA-256. Levande prøve dekkjer lik/ulik kompilator, `00`/`ff`, eksakt byteposisjon og lengdeavvik; begge wrapperane er arkiverte.
- Unified-bootstrap importerer Python-audit, compiler-samanlikning, bootstrap-diff, backend/semantic-validering og artifact-innsamling direkte som Norscode-modular. `pwd`, `mkdir`, `./bin/nc`-underprosessar og `|| true` er fjerna; isolert liveport verifiserer alle rapportgrenene.
- Single-module-probene bruker strukturert `process_spawn_argv`, eksplisitt miljø og direkte loggskriving utan `env`-shell, pipe eller `tee`. Alle tre v6800-probene er levande grøne gjennom aktiv `./bin/nc run`; den direkte native argv-flata har framleis eit separat importaliasgap og er derfor ikkje brukt som bevis for at `bin/nc` kan fjernast.
- Bootstrap-literalforventningane les og validerer no dei sju NCB-outputfilene direkte utan `pwd` eller ekstern prosess; isolert liveport verifiserer både grøn og feilande opcode.
- Real-pipeline-forventningane startar dei sju kjeldeprobene med strukturert prosess-ABI og eksplisitt miljø, utan `cd`, shellquote eller `bin/nc`-underprosess. Levande v5200-port fann alle kravde `BUILD_LIST`, `BUILD_MAP`, `INDEX_GET` og `STORE_VAR`-signal.
- Separat runtimekandidat `build/norscode_native_full_host_current_runtime_embedded_candidate_v3` har hash `42f6ee4b4677cf19a229fb4d15a9981d652ebd194bb4c694c172ff4d2df00fa2`. Dei nye målretta kontrakt-/livetest-batchane er grøne på kandidaten. Release-preflight har framleis nøyaktig éin feil, Windows stage0/kandidat-storleik.
- Windows cross-link bruker no obligatorisk LLD `/Brepro` og ei kanonisk arbeidssti før Norscode-eigd binær publisering. To komplette separate kandidatrundar gav byte-identisk hovud-PE (`dae19811fdf3c1f0fe1fa4788434538dc8f4998f1b4087f71bc4d36dbc47e20e`) og byte-identiske AppContainer-/backend-smoke-binærar; standard `build/windows` er òg identisk med desse. Committed Windows-stage0 er urørt. Den siste release-preflighten har framleis nøyaktig éin sann feil fordi stage0 er 10 682 803 byte og har VM-payload 791 621 byte, medan kandidaten er 11 033 362 byte og har VM-payload 1 122 211 byte. Dette er eit reelt generasjonsavvik som krev full Windows-attestasjon og kontrollert stage0-promotering, ikkje ei hashomgåing.
- Etter native CLI-promoteringa vart Windows-kandidaten bygd på nytt frå aktuell runtime. Ein manglande `contains_any`-kompatibilitetsfunksjon for eldre genererte snapshot vart funnen av den reelle cross-kompilatoren og reparert med featuremarkert fallback. To nye, separate bygg er byte-identiske på 11 601 573 byte med SHA-256 `244aff6a6f46b63ada99fe45811014cf522705ca475ccbf941d15aa1ac5a26f2`; backend-smoke og AppContainer-smoke er òg byte-identiske. Windows-CI køyrer no committed Windows-stage0 direkte, attesterer kandidaten før samanlikning og held stage0-promoteringa sperra til ekte Windows-bevis ligg føre.
- Alle aktive shellfiler klassifiserte som test er no fjerna. Dette omfattar kernel-boot, v795 runtime-gap, Helpdesk API-/HTTP-smoke, AI patch-vakt, feature-check, selfhost-driftsvakt, native status/readiness, plattform-readiness, pure-VM-/CLI-/aktiv runtime-gap, v735 serve-plan, random-session, bootstrap-verifisering, Omgang6/6b, seed-gate og sjølvstendighetsverifisering. Historiske reservefiler ligg under `archive/legacy_shell/`; dei aktive `.no`-eigarane står att. CLI-, Helpdesk-, AI-vakt- og releasekontraktane er grøne, og fersk active-surface er grøn på inventaret 47/47 med `test=0`.
- Byggkategorien er redusert vidare: bootstrap-regenerering, bootstrap-refresh/bisect, bootstrap-codegen-diff, bootstrap-binærgate/literalprober, bundle-entry-diagnose, v3002-kandidat, fragment-regenerering, selfhost compile-fase og L5 har ikkje lenger aktive shellinngangar. Dei målretta fragment-, host-probe-, CLI- og releasekontraktane er grøne; inventaret er 36/36 med `bygg=11`.
- Hybrid compile/bundle-wrapperane er arkiverte etter at den siste levande AOT-kallaren vart flytta til `norscode-process-spawn-v1` med lukka, eksplisitt miljøkart. Testen avdekte og låste behovet for `NORSCODE_ROOT`; macOS arm64 AOT, hybrid-reproduserbarheit, CLI-, eigarskaps- og releasekontrakt er grøne. Den ubrukte Omgang6b-shell-include-fila er òg arkivert. Inventaret er 33/33 med `bygg=8`, active-surface er grøn, og full release-preflight endar framleis nøyaktig `FEIL (1)` på det kjende Windows stage0/kandidat-avviket.
- Dei siste åtte bygg-shellfilene er arkiverte etter at søket stadfesta at ingen levande verktøy kalla dei: bootstrap-wrapper, native runtime-materialisering, Omgang6b NCB-bygg, NCB-til-ELF, L5b, L5b-mini, native ELF-selfcompile og stage0 ELF-selfcompile. Dei aktive `.no`-eigarane og CLI-rutene står att. Direkte L5b-mini er grøn med byte-identiske 721-byte-bundlar; CLI-, release-, eigarskaps- og active-surface-portane er grøne. Inventaret er 25/25 med `bygg=0`; full release-preflight har framleis berre Windows stage0/kandidat-avviket.
- Fem reine release-launcherar utan levande kallarar er arkiverte: dei to stage0-backupinngangane, cookie-rollback, promotion-readiness og kontrollert promotering. Dei aktive `.no`-eigarane og den kontrollerte promoteringspolitikken er urørte. CLI-, release- og active-surface-portane er grøne; inventaret er 20/20 med `release=6`, og full release-preflight endar framleis nøyaktig `FEIL (1)` på Windows-avviket.
- Dei siste seks release-shellinngangane er arkiverte: stage0-release-assets, stage0-seed-henting, native stage0-promotering, releasepakking, releaseinstallasjon og POSIX-bootstrapinstallasjon. Dei aktive `.no`-eigarane står att, medan installasjonsguiden definerer den signerte, attesterte prebuilt-runtimebinæren som den nødvendige bootstrapgrensa på ei ny maskin. Ferskt inventar er 14/14 med `release=0` og berre plattformkategorien att; CLI-, release-, eigarskaps- og active-surface-portane er grøne, og full release-preflight har framleis nøyaktig éin feil på Windows stage0/kandidat-avviket.
- Dei siste 14 plattformshellinngangane er arkiverte etter at kallarsøket viste at berre kontraktportane refererte til dei. Dette omfattar macOS appbygg, window-host, pakking, verifikasjon, signering, notarisering, filikon og native rebuild-probe, samt Linux kandidatbygg, stage0-Docker-bygg, Docker-pakking og tre random_hex-prober. Inventaret er no `Shellflate: 0 filer`; CLI-, release-, eigarskaps- og active-surface-portane er grøne. Full release-preflight endar framleis sannferdig med nøyaktig éin feil på Windows stage0/kandidat-storleik. Tillitsankera har uendra hash.
- `selfhost/nc_main.no` har no direkte native argv-dispatch for test/selfcheck, active-surface, surface-ownership, fase-0, drift-vakt, Python-audit, legacy-CVM-vakt, vedlikehaldsverifikasjon og LSP. Kjelde-, host-bundle- og splice-kontraktane er grøne. Den underliggjande bundlarfeilen er retta: eksplisitte `som alias`-namn blir bevarte, og feilkompilerte `builtin.<alias>.*`-kall blir normaliserte før NCB-samanslåing. Separat kandidat `6248f49f270b4e1fc38da28b0f9772c3f5860699d23c49e4b32db906d6ff8bc7` køyrer både direkte `active-surface` og ein levande importert filesystem-ABI-test utan `bin/nc`. Full liveerstatning er ikkje avhuka: source-only direktekøyring av dei største repository-gatane er framleis uakseptabelt treg og resten av CLI-argumentkontrakten står att. Den grøne v3-kandidaten og tillitsankera er urørte.
- Gjeldande v95-kandidat `build/norscode_native_crypto_candidate_v95`, hash `47e9c48f7d6d5130328b5c20ca4423a58a02a0dd76c44124f11014939180e580`, køyrer den fulle normale testflata i åtte deterministiske shardar: 783 bestått, 0 feila og 24 klassifiserte lane-/plattformhopp av 807 testtilfelle. Levande `release-preflight` endar framleis med nøyaktig `FEIL (1)`, berre Windows stage0/cross-kandidat-storleik. Kandidaten er ikkje promotert.
- Filesystem-ABI-en har no `file_replace` for atomisk publisering av ei flusha og lukka vanleg fil. POSIX brukar rotbunden `renameat`; Windows brukar validerte, reparse-avvisande handtak og `MoveFileExW(REPLACE_EXISTING|WRITE_THROUGH)`. Separat kandidat `3e05185e265e308a6369a0c46783adfc581f720d63fbac30da44546bc8d50865` består levande tempfil-utskifting og tilbakesjekk. Committed runtime og stage0 er urørte.
- Den aktive runtime-gap-porten og stage0-promoteringsfila er migrerte frå shelltekst, `exec_prosess`, `mktemp`, `rm`, `cp`, `chmod` og `mkdir` til strukturert argv, filesystem-ABI og `tools/runtime_promotion_core.no`. Active-surface og dei levande kontraktportane er grøne. Den native v143-kandidaten er no kontrollert og atomisk promotert til dist, med førre dist bevart som verifisert rollback-artefakt.
- Pure-VM-gap, vanleg CLI-gap, promotion-readiness, media-runtime og kontrollert promotering brukar no `tools.process_runner.no` med eksplisitt executable/argv/cwd/miljø, avgrensa timeout og `PATH=/nonexistent`. Dei aktive `.no`-eigarane inneheld ikkje lenger `exec_prosess`, shellquote, redirigering, `test -f`, `shasum`, `mkdir -p` eller `cp`-kommandoar.
- Den direkte hostbundlaren kanoniserer no både hovudmodul- og transitive importalias til faktisk modulsti, til dømes `tokenizer_modul.kod` til `std.tokenizer.kod`. Separat v107-kandidat består den tidlegare røde NTM1-bundleprøva og heile media-runtime-porten. Det ekstra JSON-escape-laget i overgangsmaterialiseringa blir normalisert før C-spleising; committed runtime er urørt.

## Fase 4 – Reproduserbar stage0

- [x] La committed stage0 byggje ein separat Gen1-kandidat.
- [x] La Gen1 byggje Gen2 utan maskering frå precompiled kjeldeartefaktar.
- [x] Krev byte-identisk Gen1/Gen2.
- [x] Bygg Mach-O, ELF og PE gjennom native Norscode-codegen.
- [x] Embed compiler-, VM- og runtime-NCB i éin sjølvstendig binær.
- [x] Køyr kandidaten frå tom runtime-rot med `PATH=/nonexistent`.
- [x] Køyr runtime-gap, bootstrap, L5, L5b, full test og single-binary mot kandidaten.
- [x] Promoter kandidaten atomisk først etter grøn kontrollert promoteringsport.
- [x] Behald førre stage0 som verifisert rollback-artefakt.

### Pågåande kandidatverifikasjon etter modulglobal-reparasjon

- Committed macOS-stage0 har materialisert den separate v100-Gen1-kandidaten frå 1 689 hostfunksjonar. Materialiseraren brukar no ein sjølvstendig Norscode-JSON-lesar fordi den historiske native `json_parse_raw`-brua fjerna eitt backslash-lag for mykje. Den presise quote/backslash/newline-porten er byte-identisk mellom stage0 og v95.
- Stage0-Gen1, v95-kontrollgenerasjonen og den ferdige Gen1-binæren sin Gen2-materialisering er byte-identiske: 15 695 464 byte og SHA-256 `5017a544bef76e6658e09943c2c793695c8f0a64c63e3cc517b08010087b63aa`. Separat Gen1-binær `build/norscode_native_stage0_gen1_candidate_v100` har SHA-256 `4ab1e89935e7cae00991acb2f11653b57adec84fc20d6d272cb0403ba6560169`, storleik 11 513 256 byte, og består native CLI-argumentkontrakt, Argon2-vektor og active-surface.
- Kandidaten er framleis lenka gjennom den eksplisitte C/Zig/OpenSSL-overgangsbana. Derfor er native Mach-O/ELF/PE-punktet, fase 3-promoteringa og endeleg godkjenning framleis opne; ingen tillitsanker er endra.
- Kandidat `build/norscode_native_heiltall_alias_candidate_v89` har hash `145482a1f5beac6d6a727705b55f9030fe00959d2f21891ab959cfc5a478b47c` og er ikkje promotert.
- Compiler, VM, bundler og C/AOT-runtime har eksplisitte `module_initializers`, `global_names`, `LOAD_GLOBAL` og `STORE_GLOBAL`. Native modulglobalar er GC-røter og blir relokerte ved compaction.
- Modulglobal-fixturen er grøn tre gonger med precompiled cache av og tre gonger med cache på. Direkte AOT er òg grøn etter 2500 allokeringar som pressar den globale rotlista gjennom GC.
- Native wrapper køyrer no den genererte initializer-hooken før CLI-dispatch. `nc run` bevarer modulmetadata frå hovudmodul og transitive imports.
- Blanda legacy-/ny NCB har eksplisitt strengformat per funksjon. Generatoren skil rå VM-JSON frå ferdig dekoda native JSON utan heuristisk dobbel-unescape.
- API-smoke, admin-relasjonar, DB-feature, DB-repository og dei fokuserte modulglobal-/host-bundle-kontraktane er grøne mot v89.
- L5, full L5b og L5b-mini er grøne tre gonger med cache av og på. Cacheartefakta vart først regenererte etter grøn source-only L5b. Full normaltest, slow-lane, bootstrap og strict-port er ikkje køyrde på nytt mot v89 enno, så den komplette kandidatmatrisa står sannferdig open.
- Tillitsankera er framleis urørte: `dist/norscode_native` og `bootstrap/stage0/norscode-macos-arm64` har begge hash `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`.
- Gjeldande verifikasjonskandidat er `build/norscode_native_crypto_candidate_v93`, 11 377 416 byte med hash `c9be3ae0147ec314cc307ac5a600702299327a7ac799399757a3117126189eb7`. Han er ikkje promoterbar: Argon2 går gjennom eksplisitt Zig-adapter og TLS/ACME gjennom statisk OpenSSL, og Windows stage0/kandidat-avviket er framleis reelt.
- Release-preflight-kontraktane er synkroniserte med den strukturerte Norscode-prosessflata. Normal preflight er redusert frå 43 foreldede feil til éi sann sperre: Windows stage0 og cross-kandidat har ulik storleik. `local-green --strict` stoppar korrekt i fyrste steg; strict sporing legg i tillegg til uspora arbeidskopifiler fram til endringane er committed.
- `tools/runtime_promotion_core.no` bevarer eksisterande mål som innhaldsadressert rollback-artefakt, verifiserer kandidat- og rollback-SHA-256, set køyrbar modus gjennom filesystem-ABI og publiserer med atomisk `file_replace`. Ei levande binær fixture promoterer, gjenopprettar og avviser manipulert rollback utan å endre målet. `promote_native_stage0_v3001.no` køyrer runtime-gap-porten med executable/argv og har ein grøn kontrollert dry-run for både dist og macOS-stage0. Begge tillitsankera har framleis uendra SHA-256 `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`; rollback- og promoteringspunkta står opne til ei lovleg kandidatpromotering faktisk blir gjennomført.
- Kontrollert v3005-dry-run er grøn mot `build/norscode_native_canonical_alias_candidate_v107`: kandidat-gap, filesystem-list, alle 20 media-/modellprøver, pure-VM-gap, CLI-gap, readiness og separate dist/stage0-promoteringsprøver består. Rapporten viser `dry_run_green_no_promotion`; dist og macOS-stage0 har uendra hash `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`. Native `sha256_bytes` blir brukt for store binærar med rein Norscode-fallback, slik at kontrollen ikkje fastnar i fleire minutt per 11,5 MB-fil.
- Native `bygg-native` er no eigd av `selfhost/nc_main.no` med eksplisitt target/argv-dispatch, direkte Norscode-NCB-hostkøyring og filesystem-ABI for køyrbar modus; dei migrerte AOT-testane brukar ikkje `bin/nc`, shelltekst eller `exec_prosess`. Kandidat v110, `build/norscode_native_temp_mode_candidate_v110`, er 11 789 480 byte med SHA-256 `d19bb853fc06ac30c95d92eb1baf2eb2a979333d73a5a52db4b099205b125c88`. På denne kandidaten er signert og faktisk køyrd Mach-O arm64, Mach-O-funksjonskall/import/kontrollflyt/liste-/tekstoperasjonar, ELF64 x86-64, ELF64 AArch64 og PE32+-emitteren grøne. Darwin `/tmp` blir normalisert til den kjende `/private/tmp`-rota før `O_NOFOLLOW`-vandringa; vilkårlege symlenker er framleis fail-closed. Tillitsankera er urørte med SHA-256 `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`.
- Windows-stage0 er no promotert gjennom `tools/promote_attested_windows_stage0.no` etter signert GitHub-attestasjon, kjeldecommitkontroll og kandidat-SHA-256. Promoteringskjernen publiserte atomisk, oppdaterte stage0-manifestet og bevarte både den opphavlege stage0-en `4e30aef1...b4bed` og den fyrste attesterte generasjonen `789ba729...b23f0d` som permanente, hashverifiserte rollback-artefaktar under `bootstrap/stage0/rollback/`.

## Fase 5 – Native Linux-, macOS- og Windows-bygg

- [ ] Fjern aktiv GCC/Zig-orkestrering frå Linux-bygg.
- [ ] Fjern aktive C-bundlarar frå Windows PE/runtime-bygg.
- [x] Reparer Windows stage0/kandidat-avviket og krev identisk hash.
- [ ] Fullfør filesystem-, prosess-, nettverk-, tråd- og sikkerheits-ABI-ar.
- [ ] Køyr native Linux x86-64 og ARM64 utan Docker-byggeverktøy i normalflyten.
- [x] Køyr ekte Windows-attestasjon for SChannel, AppContainer, IOCP, prosess og Argon2id.
- [x] Køyr macOS-attestasjon for signering, sandbox, TLS og native runtime.
- [ ] Set `production_ready_all_platforms=true` berre når alle tre er attesterte.

### Fase 5-evidens

- Windows cross-kandidat A og B er byte-identiske: begge er 11 033 362 byte med SHA-256 `dae19811fdf3c1f0fe1fa4788434538dc8f4998f1b4087f71bc4d36dbc47e20e`. Committed Windows-stage0 er 10 682 803 byte med SHA-256 `4e30aef18e037e2eff20b7307908cc5328e7842e6e1faa6a1340bae48a9b4bed`; avviket er dermed ein reell promoterings-/attestasjonsgrense, ikkje eit nondeterministisk cross-bygg.
- `tools/windows_runtime_attestation.no` er migrert bort frå `bash.exe`, `awk`, `mkdir -p`, Git-tekstkommandoar og `builtin.exec_prosess`. Ho brukar no systeminfo-ABI, Norscode SHA-256, filesystem-ABI og strukturert executable/argv, og bind rapporten til `GITHUB_SHA` eller eksplisitt `NORSCODE_SOURCE_COMMIT`.
- `tools/platform_readiness_v3600.no` er òg shellfri: runtime-smoke brukar avgrensa tempområde og direkte argv, binærhash går gjennom native Norscode SHA-256-ABI, og GitHub/Sigstore-verifisering blir starta som strukturert `gh`-prosess. Den levande porten rapporterer `production_ready_all_platforms=false` til signerte attestasjonar faktisk finst.
- Kildekontroll og den direkte shellfrie Windows-attestasjonskontrakten er grøne på macOS. Sjølve SChannel/AppContainer/IOCP/Argon2id-attestasjonen står framleis open fordi planen krev faktisk Windows x86_64-host; kryssbygg eller Wine skal ikkje reknast som bevis.
- Ikkje-promotert v94-kandidat, hash `28d3d5dc3bfde74bd2348478a2974738ade224be3baaa6a130b499f23056e1e2` og storleik 11 486 392 byte, er materialisert frå ein komplett 1 688-funksjons host-graf. Frå `tests/` og utan `NORSCODE_ROOT` normaliserer kandidaten relativ executable-sti mot `cwd`, finn repo-rota og køyrer `active-surface` grønt. Importerande fokustestar køyrer òg grønt etter at dei `decoded-v1`-merkte funksjonane vart materialiserte i korrekt rå-parsermodus. Dette beviser native dispatch og rotfinning i kandidaten, men `bin/nc`-avhukinga står open fordi v94 framleis har den eksplisitte Zig-Argon2/OpenSSL-overgangsbackend-en og ikkje er lovleg promotert.
- V95 legg levande `selfhost/vm.no` over hostgrafen og materialiserer 1 689 funksjonar. Kryptokandidaten har hash `47e9c48f7d6d5130328b5c20ca4423a58a02a0dd76c44124f11014939180e580`, storleik 11 519 384 byte, og køyrer Argon2id-vektoren, VM-sikkerheitsforwarding og executable-root-kontrakten grønt. Materialisatoren har samstundes fått eigen desimalparser, slik at v95 sjølv kan lese funksjonsintervall utan den tidlegare `heiltall_fra_tekst`-feilen.
- CI-køyring `30749220401` produserte og signerte ei ekte Windows x86-64-attestasjon for den kanoniske PE-kandidaten med SHA-256 `d1917398a0c0b026dc9e3a4d0ae2163fa2e7863d1b428ccc9cb2b4b58cb98298`. Rapporten bind SChannel TLS 1.3, AppContainer, IOCP, filesystem, prosess, Argon2id og ACME-signering/verifisering til kjeldecommit `42641f778c0a053816670079410015cdaedf7a81`.
- LLD `/Brepro` åleine var ikkje stabil fordi PE-tidsstempel, CodeView-GUID og PDB-sti varierte mellom ellers identiske køyringar. `tools/normalize_pe_reproducible.no` kanoniserer desse ikkje-funksjonelle felta strukturert i Norscode. To komplette lokale bygg gav byte-identisk hovudruntime `d1917398...98298`, backend-smoke `43bfbdd7...c939a` og AppContainer-smoke `ed247179...5e7264`; CI gav dei same tre hashane.
- Etter promotering av den kanoniske kandidaten bygde CI-køyring `30749456344` ein ny kandidat med same SHA-256 `d1917398a0c0b026dc9e3a4d0ae2163fa2e7863d1b428ccc9cb2b4b58cb98298`. Heile køyringa er grøn, og Windows-jobben stadfesta tracked stage0, ekte runtime-attestasjon, signert provenance, samla plattformreadiness og byte-identisk stage0/kandidat.
- CI-køyring `30750405335`, jobb `91503362573`, køyrde kandidaten på ein ekte GitHub-hosted Darwin ARM64-runner. Han bestod native selftest, Norscode-generert og ad-hoc-signert Mach-O AOT, Seatbelt-sandbox og TLS 1.3 med CRL, OCSP og mTLS. GitHub signerte rapporten med build provenance, og fail-closed plattformreadiness verifiserte både signaturen, kjeldecommit `06dcff51fe8902a7b08c7ea3e692e4653ffa50ee` og kandidat-SHA-256 `933003b594d8ed4adf0f6ae2116ec9c3bc1986cf919af3c295f8c310d7dff796`.
- CI har no ein eigen fail-closed `all-platform-runtime-attestation`-jobb. Han lastar ned dei fire signerte macOS-, Linux x86-64-, Linux ARM64- og Windows-artefakta, bind alle rapportane til same kjeldecommit og krev at den levande Norscode-porten faktisk skriv `production_ready_all_platforms=true`. Punktet blir ståande ope til denne samla jobben er grøn.
- ARM64-lana køyrer den kjeldebygde proben direkte i committed ARM64-stage0 i staden for å starte ein ny VM inne i VM-en. Filesystem/prosess/tråd, Argon2id og TLS/epoll er separate fail-closed steg med treminuttsgrense; rapporten blir berre skriven etter at alle tre er grøne. Dette fjernar den observerte henginga i den eldre ARM-prosessventinga og gjer kvar backend synleg i CI. Attestasjonspunktet står ope til denne nye delte lana er signert grøn.

## Fase 6 – Yting

- [x] Legg inn faste benchmarkar for kald/varm kompilering, L5b, testtid, minnetopp og GC-pausar.
- [x] Implementer innhaldsadressert og ABI-versjonert modulcache.
- [x] Implementer inkrementell kompilering av berre endra modular.
- [x] Parallelliser uavhengig kompilering og testing deterministisk.
- [x] Stream store NCB-filer for å redusere minnetopp.
- [x] Optimaliser bytekodestack og fjern unødvendige push/pop.
- [x] Optimaliser GC frå profilerte røter og allokeringar.
- [x] Bruk JIT/AOT automatisk for målte varme funksjonar med trygg VM-fallback.
- [x] Oppnå minst 2 gonger raskare varm bygging.
- [x] Oppnå minst 30 prosent raskare kald bygging.
- [x] Reduser L5b-minnetoppen med minst 40 prosent.
- [x] Avvis funksjonelle eller deterministiske ytingsregresjonar.

### Fase 6-evidens

- Automatisk JIT er låst av `tests/test_vm_jit_automatic_fallback_contract.no`: VM-en tel kall per funksjon/type, kompilerer først ved varmterskelen (standard 50), måler kompilatorfeil og deoptimaliseringar, nullstiller ugyldige handtak og held fram i den vanlege VM-kallstien. Med terskel 2 er heiltals-, desimal- og tekstintegrasjonstestane køyrde grøne mot rein, aktuell selfhost-VM via v121-kandidaten; testane krev faktiske `jit_compiles` og `jit_calls`, og eksplisitt `jit.execute`-capability. Release-preflight krev både kontrakttesten, varmterskelen og fallback-markørane.
- Bytekodegeneratoren fjernar no den semantisk tomme sekvensen `PUSH_CONST <literal>; POP` når ein literal står åleine som setning. Optimaliseringa skjer før kjeldekartet blir bygd og rører ikkje kall eller andre uttrykk med moglege bivirkningar. `tests/test_bytecode_unused_literal_stack.no` beviser både at literalparet er borte og at `CALL; POP` blir bevart; testen er grøn mot v121-kandidaten, og release-preflight krev implementasjonen og livetesten.
- GC-en brukar no dei profilerte heap-ID-ane frå operandstakkar og lokale variablar til å deduplisere røter før `runtime_memory` tek lås og gjer lineære rotoppslag. Metrikkane `gc_root_candidates`, `gc_root_unique` og `gc_root_duplicates_elided` viser effekten saman med eksisterande allokerings-/live-byte-metrikkar. `tests/test_vm_gc_root_dedup.no` held tre alias til same liste levande gjennom automatisk GC, krev at duplikat faktisk blir fjerna og verifiserer at objektet overlever; testen er grøn mot ein nykompilert aktuell VM-host på v121-kandidaten.
- Store NCB-bundle har no eit versjonert, postbasert format `norscode-ncb-stream-v1`: metadata og kvar funksjon blir skrivne direkte gjennom native fil-ABI utan ein ekstra heilbundle-tekstkopi. VM-executor les formatet i avgrensa bitar og materialiserer kvar funksjons JSON fyrst ved første oppslag. Rundturstesten krev deterministiske, byte-identiske filer og fleire lesebitar; ein levande 589-postars fixture køyrde med exit 0 i både JSON- og straumformat. Den strukturerte Norscode-benchmarken målte topp-RSS frå 104 775 680 til 99 975 168 byte, ein reduksjon på 4,58 prosent, og release-preflight låser skrivar, lazy-lesar, VM-materialisering og fail-closed måling. Dette hukar av sjølve straumdelmålet, ikkje det separate kravet om 40 prosent lågare L5b-minnetopp.
- L5b-minnekravet er målt mot den registrerte full-v114-baselinen på 67 452 928 byte. Tre separate v121-køyringar i den nye `l5b`-benchmarkmodusen brukte 33 439 744, 33 652 736 og 33 603 584 byte maksimal RSS, alle med grøn L5b og 50,11–50,43 prosent reduksjon. Benchmarken rapporterer no baseline, reduksjon i basispunkt og `l5b_40_percent_target`, og feilar lukka under 4 000 basispunkt i både `l5b`- og fullmodus.
- `nc compile` brukar no den same innhaldsadresserte, ABI-versjonerte og atomisk publiserte cachen som native verktøydispatch. Modulnamnet er del av nøkkelen, og importerte modular blir bundla både ved cachebom og utan cache. Separat v122-kandidat, `build/norscode_native_compile_cache_candidate_v122`, er 11 663 040 byte med SHA-256 `8909fe5b5da0279e4e7f37efde4aa9b79345e39afb4cbfdf98b515481fcbcd87`. Tre fail-closed quickrapportar målte kald/varm kompilering til 235/90, 245/98 og 244/98 ms, tilsvarande 2,49–2,61 gonger raskare varm bygging. Alle seks artefaktar var byte-identiske med SHA-256 `41d92239441b518cdcdf7889c0b09c1db458d2209a321a843edd56336902743e`, og den produserte NCB-en køyrde med exit 0 i rein VM. Benchmarken krev no minst 20 000 basispunkt og rapporterer `warm_compile_2x_target`.
- Kaldbyggløypa har no committed, kildeverifiserte stdlib-fragment for `std.sha256` og `std.tekst`. Manifestet bind ABI, modulnamn, kjelde-SHA-256 og artefakt-SHA-256; kvart avvik fell attende til levande kompilering. `NORSCODE_USE_PRECOMPILED_STDLIB=0` og den vanlege source-only bootstrapmodusen koplar fragmenta heilt ut. Separat v125-kandidat, `build/norscode_native_stdlib_cache_candidate_v125`, er 11 702 424 byte med SHA-256 `674f5aa6d64fe4c858c1c3f0e4011bac647440a3f81ebda95cb9cb721562478f`. Tre fail-closed rapportar målte rein kjeldereferanse/kald bygging til 232/104, 244/109 og 244/109 ms, ein reduksjon på 55,17–55,32 prosent. Kald, varm og rein kjelde produserte alle byte-identisk NCB med SHA-256 `41d92239441b518cdcdf7889c0b09c1db458d2209a321a843edd56336902743e`; NCB-en køyrde med exit 0. Benchmarken krev no minst 3 000 basispunkt og rapporterer `cold_compile_30_percent_target`.

- `tools/performance_benchmark.no` definerer no eit versjonert, maskinlesbart fastsett for kald/varm kompilering, L5b, full test, topp-RSS og GC-pause. Native prosess-ABI rapporterer `wall_ms` og sampla `peak_rss_bytes` på POSIX; Windows les `PeakProcessMemoryUsed` frå Job Object.
- Separat v121-kandidat, `build/norscode_native_cache_validation_candidate_v121`, er 11 686 288 byte med SHA-256 `9e056c19156b72796115e0e42a25ff919c12ad930848ca5ba3ddbf66f01d1696`. Levande fullrapport `build/benchmarks/full-v121b.json` er grøn: kald kompilering 195 ms / 23 609 344 byte RSS, varm kompilering 188 ms / 23 576 576 byte RSS, L5b 9 331 ms / 40 583 168 byte RSS, full åttevegs test 586 787 ms / 745 046 016 byte summerte shard-toppar og GC-probe 12 438 ms / 132 595 712 byte RSS med 4 116 ms rapportert GC-pause. Kald og varm NCB er byte-identiske med SHA-256 `32935e52795ca80f3e3b3b3b8c1cf2747ad8202c4171a1604d03c46daee6c8c8`.
- Den native verktøycachen vart levande verifisert fail-closed på v121: første køyring publiserte cache-ABI v3 atomisk, og andre køyring gav treff på same innhaldsadresserte NCB. Eldre metadata-lause treff blir ikkje gjenbrukte.
- Benchmarken returnerer feil dersom L5b eller full test er raud, eller dersom kald og varm kompilering ikkje gir same artefakthash. Den grøne fullrapporten beviser dermed både funksjons- og determinismeporten; kontrakttesten låser fail-closed-vilkåra.
- Dei tidlegare fulltest-timeoutane vart reparerte ved å flytte HTTP-responsserialisering til den smale produksjonsmodulen `selfhost/http_response.no`; sju mikrotestar importerer ikkje lenger heile CLI-/kompilatorgrafen. Den store CLI-argumenttesten passerer funksjonelt på 108,02 s utan cache og deretter gjennom den varme, innhaldsadresserte testcachen.
- Modul- og testcache brukar SHA-256 over kjeldeinnhald og eksplisitt ABI-versjon, ikkje runtime-metadata.
- Bundleren har no opt-in `NORSCODE_INCREMENTAL_MODULE_CACHE=1` med ABI `norscode-incremental-module-cache-v1`. Nøkkelen bind modulnamn, modulens kjeldehash og SHA-256 over den aktive kompilatorgrafen. Levande `tests/test_incremental_module_cache_live.no` endrar berre modul A og krev to kompileringar av A, éi kompilering av B og eitt cachetreff for uendra B; testen er grøn gjennom strukturert Norscode-prosess-ABI.
- Ei endring i éin test rekompilerte berre den testen; neste komplette normalport brukte den materialiserte cachen og gjekk frå 3 104,97 s kaldt til 159,15 s varmt.
- Åtteveis normal- og slow-lane er begge grøne med stabil sortering, indeks-modulo-sharding og strukturert native prosess-ABI.
- Native direkteverktøy i `selfhost/nc_main.no` har i tillegg ein eigen innhaldsadressert NCB-cache. Nøkkelen er SHA-256 over eksplisitt cache/compiler/NCB-ABI, normaliserte repo-relative filstiar og heile den transitive importgrafen. Ugyldige treff blir forkasta fail-closed; cachefeil fell ope tilbake til korrekt kompilering.
- Eksplisitte enkeltfiltestar unngår no heiltre-hash som standard; `NC_TEST_CACHE=1` vel inn innhaldsadressert enkeltfilcache. Dette fjerna den identifiserte pre-test-flaskehalsen, men v22-profilen viser at heapsporing i VM-i-VM-kompilering framleis må optimaliserast før varm native CLI-testing kan godkjennast.
- Repoimportløysaren behandlar no `tools/`, `tests/`, `packages/` og `prosjekter/` som rot-relative modular. Det reparerer både cachefingerprint og den tidlegare manglande `ownership.script_eigar_gate` i direkte fase-0-bundle.
- Kandidat `build/norscode_native_native_cache_candidate_v6`, hash `c59ee745d63f5bb68feb1f06b387cb25a2452bd27d59e8aec27967b0a6608ec7`, publiserte phase0-NCB atomisk som `ca44e500...ncb.json` og gav identisk cachetreff på under eitt sekund før gateutføring. Cachefila er 226 KiB og har hash `4f6daf8b014aaecd781386bc65f3859d47ee7fe8acd5874cd81348981acee2e0`.
- Full direkte phase0 er no grøn på v13. Native suffiks-/prune-vandring, map-oppslag for C-allowlist og samla `contains_any` reduserte den verifiserte kandidatmålinga frå ein tidlegare avbroten 292-sekundsbane til 36,00 s ved cachebom; vidare AOT/VM-optimalisering står framleis open.
- Eksplisitt NCB-bunden fastmodus for native CLI-verktøy reduserte fase0 frå v15 sine 38,43 s til v23 sine 12,83 s (66,6 prosent), og active-surface frå 41,51 s til 13,65 s (67,1 prosent), med grøne porter. Testmål overstyrer framleis til full instrumentering. Dette er gateyting, ikkje bevis for dei separate varm-/kaldbyggmåla.
- C-materialisatoren deler no builtin-aliasregisteret gjennom éi heil generering og skriv opptil 256 instruksjonar per avgrensa appendblokk. Han støttar i tillegg eksplisitte, deterministiske funksjonsintervall med komplett global kallgraf. 17 avgrensa intervall vart komponerte av `tools/maint/compose_generated_c_batches.no`, som fail-closed kravde nøyaktig 1 688 definisjonar før v94 fekk kompilerast. Dette løyser den praktiske fullmaterialiseringsblokkaren og avgrensar kvar generatorprosess, men er ikkje i seg sjølv prosentbevis for kaldbygg- eller minnemåla.
- Målet for kald byggjetid er no grønt mot ein fast, semantisk lik kjeldereferanse: tre v125-rundar gav 55,17–55,32 prosent reduksjon med identiske NCB-bytes. Varm bygging, kald bygging og L5b-minne har tre målingar kvar og fail-closed prosentportar.
- Fersk v93-strict brukte 3 554,78 s og 4 687 577 088 byte maksimal RSS. Normalflata var funksjonelt grøn, men kaldkjøringa tok om lag 46 minutt; ho er ein registrert ytingsregresjon og kan ikkje brukast som bevis for kaldbyggmålet.

## Fase 7 – Fjern eksterne runtimebibliotek

- [ ] Gjer NorsDB til standard og SQLite til valfri migreringsadapter.
- [x] Gjer rein Norscode TAR/gzip til standard.
- [x] Implementer Norscode Argon2id med offisielle testvektorar og konstanttidskontroll.
- [ ] Implementer Norscode TLS/krypto med differentialtesting.
- [ ] Gjennomfør ekstern sikkerheitsrevisjon før OpenSSL blir fjerna som standard.
- [ ] Fjern SQLite-, OpenSSL- og Zig-avhengigheiter først etter grøne portar.
- [ ] Behald eksterne bibliotek berre som eksplisitte valfrie adapterar.

### Fase 7-evidens

- `std.shutil` annonserer no berre `tar` og `gztar` som standard arkivformat og køyrer begge direkte gjennom `std.tarfile`/`std.gzip`; ukjende format feilar lukka utan ekstern reserveprosess. Den levande standard-API-porten pakkar og pakkar ut både tekst og binærdata gjennom TAR og TAR.GZ, medan dei separate gzip-, tarfile-, shutil- og shellfridomsportane òg er grøne mot v100-Gen1-kandidaten.
- POSIX-overgangsruntimen kan no byggjast med `NC_DYNAMIC_SQLITE`, same eksplisitte adaptergrense som Windows allereie hadde. SQLite-symbol blir då lasta ved behov med `dlopen`/`dlsym`, og kandidaten har inga direkte SQLite-linkavhengigheit.
- Kandidat `build/norscode_native_candidate_v95_no_external`, hash `f8d4d5f83ca0ee81abcb8ed826c45e4f4ca3e9765c21a54787e4e3c75043c755` og storleik 11 495 256 byte, er bygd utan OpenSSL, Zig-objekt og direkte SQLite-link. `otool -L` viser berre macOS-systembibliotek og -rammeverk; importtest og `active-surface` er grøne.
- Ny v126-kandidat byggjer den aktuelle v125-kompilator-/cachekjelda med `NC_DYNAMIC_SQLITE`, utan OpenSSL-flagget og utan Zig-objekt. `build/norscode_native_platform_only_candidate_v126` er 11 678 208 byte med SHA-256 `60aec0d65b1eddacfe50b27c6f7c740126052f95cececf0ae55d54299c50d3fc`; `otool -L` viser berre macOS-systembibliotek og -rammeverk. Native hjelp, `active-surface`, databasegrunnport, databasefunksjonsport, dynamisk SQLite-adapterkontrakt og stdlib source/cache-liveport er grøne.
- `std.blake2b` og `std.argon2id_pure` implementerer no BLAKE2b og Argon2id v1.3 i reine `.no`-filer. RFC 9106-vektoren med 32 KiB, tre pass, fire lanes, secret og associated data gir byte-identisk tag `0d640df58d78766c08c037a34a8b53c9d01ef0452d75b65eb52520e96b01e659`; prehash-digesten blir òg kontrollert. Standard-API-et `std.argon2id` brukar den reine implementasjonen, verifisering brukar ein full-lengde XOR/OR-kontroll, og `builtin.argon2id` er berre ein eksplisitt valfri adapter. BLAKE2b-vektorar, RFC-vektor, standard-API, konstanttidskontrakt og differentialtest mot v125 sin etablerte native backend er grøne; v126 passerer alle reine testar utan OpenSSL eller Zig.
- Kandidatmatrisa for v126 har grøne `active-surface`, reparert `phase0-verify`, L5b-mini, bootstrap A+B/C og normal `selvstendighet`. L5 materialiserte 1 945 610 byte identisk Gen1/Gen2. Fase-0 eigarskap skil no genererte, vilkårlege `build/*.c`-mellomfiler frå aktiv kjelde, men kontrollerer framleis dei eksplisitte committed historiske C-artefaktane og test-/vendor-unntaka. Release-preflight med lokal sporingskontroll avslått passerte 2 826 kontrollar og stoppa på éin sannferdig feil: committed Windows-stage0 og cross-kandidaten har ulik storleik. Ingen tillitsanker er promoterte.
- Gjeldande ikkje-promoterte v127-kandidat `build/norscode_native_platform_only_candidate_v127` er 6 274 528 byte med SHA-256 `c9f75a170bf35c02ba1d9f1cb91c64a3a4feaa4ee35a1748dbb6bff204aea9f0`. Han har berre macOS-systemrammeverk/-bibliotek, og består `active-surface`, `phase0-verify`, normal sjølvstende, L5, L5b-mini, full L5b med cache på og av, RFC 9106 Argon2id, BLAKE2b, GC-rootdeduplisering og direkte CLI-argv/miljø. Dei fire testane som krev ein valfri native kryptoadapter er no eksplisitt klassifiserte som `optional-native-crypto-adapter`; dette er ikkje brukt som bevis for rein TLS/ACME eller ekstern sikkerheitsrevisjon. Tillitsankera er framleis urørte med SHA-256 `97483d04c3a433297d152b2788176828e04d4e98c4d7f9b41a6b3597fa269284`.

## Endeleg godkjenning

- [x] `./bin/nc active-surface`
- [ ] `./bin/nc selvstendighet --strict`
- [x] `./bin/nc selfcompile-l5b` med og utan cache
- [ ] `./bin/nc local-green --strict`
- [x] `./bin/nc release-preflight`
- [x] Alle testar og slow-lane-testar utan uklassifiserte hopp
- [ ] Byte-identisk Gen1/Gen2 på alle støtta plattformer
- [ ] Ingen aktive `.sh`, `.py`, `.c`, `.h` eller Zig-kjelder i normal bygg-/release-/CI-flate
- [ ] Ingen eksterne prosessar for operasjonar Norscode sjølv støttar
- [ ] Signert attestasjon frå macOS, Linux og ekte Windows
- [ ] Dokumentasjon og statusmatriser samsvarer med levande portar
