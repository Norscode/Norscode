funksjon start() -> heiltall {
    la res = builtin.exec_prosess("python3 -c 'import secrets; print(secrets.token_hex(8))'")
    skriv("kode=" + tekst(res[0]) + "\n")
    skriv("ut=" + tekst(res[1]) + "\n")
    returner 0
}
