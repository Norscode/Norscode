// tools/fixtures/diff_optional.no — Omgang 5 oppgåve 2: optional-chaining differensial.
// Testar M10 o?.felt i native codegen (DUP + COMPARE_EQ ingenting + INDEX_GET).
// Forventa exit = 42.
funksjon start() -> heiltall {
    la m = {"x": 42}
    la a = m?.x           // 42 (m ikkje ingenting)
    returner a
}
