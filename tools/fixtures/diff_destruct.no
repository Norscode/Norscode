// tools/fixtures/diff_destruct.no — Omgang 5 oppgåve 2: destrukturering differensial.
// Testar M10 la [a,b] = liste i native codegen (INDEX_GET, alt støtta). exit = 42.
funksjon start() -> heiltall {
    la [a, b] = [40, 2]
    returner a + b        // 42
}
