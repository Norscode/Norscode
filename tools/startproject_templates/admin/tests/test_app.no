bruk std.test som test
import app

funksjon test_start() {
    la r = app.start()
    assert_eq(r, 0)
}

funksjon start() -> heiltall {
    la testar = ["test_start"]
    returner test.køyr(testar)
}
