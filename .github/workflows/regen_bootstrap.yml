# Historisk / opt-in workflow for seed-vedlikehald og deterministisk regen.
# Dette er ikkje normal build/test/CI-flyt og skal ikkje lesast som driftskrav.
# Workflowen verifiserer den isolerte vedlikehaldsflata for historisk seed-fornying og samanlikning.
name: Regen bootstrap

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'selfhost/**'
      - 'bootstrap/**'
      - 'tools/maint/**'
      - 'archive/legacy_c_backend/**'

env:
  GITHUB_TOKEN: ${{ github.token }}

concurrency:
  group: regen-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  regen-verify:
    name: Regen-verify (Linux historical-maintenance)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v6

      - name: Installer clang
        run: sudo apt-get update && sudo apt-get install -y clang libsqlite3-dev

      - name: Stage-0 seed
        run: bash tools/ensure_stage0_seed.sh

      - name: Bygg stage-0 (isolert historisk løype)
        run: NORSCODE_BOOTSTRAP_C=1 REGEN=1 bash tools/build_norscode_native.sh

      - name: Regenerer og samanlikn historisk output
        run: bash tools/regen_verify.sh

      - name: nc_main host-smoke (historisk / opt-in)
        run: bash tools/verify_nc_main_host.sh
