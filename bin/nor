#!/usr/bin/env sh
set -eu

ROOT_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
NC="$ROOT_DIR/nc"

if [ -x "$NC" ]; then
  exec "$NC" "$@"
fi

printf 'Norscode CLI not found at: %s\n' "$NC" >&2
printf 'Bruk ./bin/nc for normal CLI.\n' >&2
exit 1
