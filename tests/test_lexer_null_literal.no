bruk selfhost.lexer.lexer_m1 som lexer

funksjon start() -> heiltall {
    la tokens = lexer.lex("null ingenting")
    assert_eq(tokens[0]["type"], "NULL")
    assert_eq(tokens[1]["type"], "NULL")
    skriv("test_lexer_null_literal OK")
    returner 0
}
