bruk std.tekst som t

funksjon start() -> heiltall {
    la src = fil_les("tools/probe_random_hex_runtime_v712.no")

    assert(t.inneholder(src, "funksjon vis_kildetreff(mapper: tekst, needles: liste_tekst, maks: heiltall) -> heiltall"))
    assert(t.inneholder(src, "funksjon søkefiler(mapper: tekst) -> liste_tekst"))
    assert(t.inneholder(src, "native_fs.native_list(\".\", relativ_katalog)"))
    assert(t.inneholder(src, "native_fs.native_list(\".\", fil)"))
    assert(t.inneholder(src, "ikkje har_fil(filer, fil)"))
    assert(t.inneholder(src, "builtin.fil_les(sti)"))
    assert(t.inneholder(src, "vis_kildetreff(\"std selfhost src tools bin\", random_ord, 160)"))
    assert(t.inneholder(src, "vis_kildetreff(\"selfhost src tools bin std\", builtin_ord, 200)"))
    assert(t.inneholder(src, "vis_kildetreff(\"selfhost src tools bin\", serve_ord, 200)"))
    assert(ikkje t.inneholder(src, "exec_prosess"))
    assert(ikkje t.inneholder(src, "builtin.exec_prosess"))
    assert(ikkje t.inneholder(src, "/usr/bin/grep -R -n"))
    assert(ikkje t.inneholder(src, "find "))
    assert(ikkje t.inneholder(src, "| head -160"))
    assert(ikkje t.inneholder(src, "| head -200"))

    skriv("test_probe_random_hex_runtime_no_grep_head OK")
    returner 0
}
