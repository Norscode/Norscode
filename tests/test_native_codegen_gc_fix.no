// F3.8-port: fail-closed innskriving av GC-kallstadene i native_codegen_v2 (gc_fix_løys).
// Køyrer tests/fixtures/ncg_f3_gcfix_probe.no: alle fem cache-fallbackane skal skrivast rett,
// ein MANGLANDE stad (rå bump-allokator i GC-modus) og ei dobbel innskriving skal bli AVVISTE,
// og flagg AV skal ikkje røre bytane. Tung compile (importerer heile kodegeneratoren):
// klassifisert i nc_test.er_tung_compile og køyrd i GC-matrisa (tools/ci_native_gc.no).
bruk tests.fixtures.ncg_f3_gcfix_probe som probe

funksjon start() -> heiltall {
    assert_eq(probe.start(), 0)
    skriv("test_native_codegen_gc_fix OK\n")
    returner 0
}
