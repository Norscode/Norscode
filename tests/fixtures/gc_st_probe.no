funksjon start() -> heiltall {
    la l: liste_heiltall = [1, 2, 3]
    la t = builtin.slice(l, 0, 2)
    skriv("type=" + builtin.type(t) + "\n")
    skriv("len=" + tekst(lengde(t)) + "\n")
    returner 0
}
