funksjon lag_verdi(x: heiltall) {
    la values = [x, x + 1]
    la meta = {"count": 2}
    hvis lengde(values) == meta["count"] { returner 0 }
    returner 1
}

funksjon start() -> heiltall {
    returner lag_verdi(4)
}
