// AOT (linux-x86_64): ufanga assert_eq-feil skal stoppe programmet med exit 1 og
// «assert_eq feilet: 3 != 4» på stderr (før: null-fallback, so testen «stod»).
funksjon start() -> heltall {
    assert_eq(1, 1)
    assert_eq(3, 4)
    skriv("skulle ikkje nå hit\n")
    returner 0
}
