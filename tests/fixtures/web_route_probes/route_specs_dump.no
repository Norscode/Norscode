bruk std.web som web

funksjon start() -> heiltall {
    la rh = ncb_route_handlers()
    la i: heiltall = 0
    mens i < lengde(rh) {
        la handler = rh[i]
        la spec = handler["spec"]
        skriv(spec)
        skriv("\n")
        i = i + 1
    }
    returner 0
}
