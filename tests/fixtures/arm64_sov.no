// vent.sov(ms) må faktisk sove (macOS: poll #230, Linux: nanosleep #101). exit 0 = OK.
funksjon start() -> heiltall {
    la t0 = builtin.tid_ms()
    builtin.vent.sov(300)
    la brukt = builtin.tid_ms() - t0
    hvis brukt >= 200 { exit(0) }
    exit(1)
    returner 7
}
