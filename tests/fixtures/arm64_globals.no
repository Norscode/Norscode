la teller = 0
la sum = 100

funksjon auk() -> heiltall { teller = teller + 1 sum = sum + teller returner teller }

funksjon start() -> heiltall {
    auk()
    auk()
    auk()
    returner teller + sum
}
