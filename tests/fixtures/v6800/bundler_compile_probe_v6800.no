bruk selfhost.bundler

funksjon start() -> heiltall {
    la ncb = les_ncb_eller_kompiler("selfhost/lexer/lexer_m1.no")
    skriv("compiled_len=" + tekst_fra_heltall(lengde(ncb)) + "\n")
    returner 0
}
