bruk std.sha256 som sha256
bruk std.tekst som tekstverk

funksjon start() -> heiltall {
    la verdi = tekstverk.trim("  Norscode benchmark  ")
    assert(lengde(sha256.hash(verdi)) == 64)
    returner 0
}
