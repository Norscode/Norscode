funksjon start() -> heiltall {
    la data: liste<ordbok_tekst> = []
    la i: heiltall = 0
    mens i < 2000 {
        legg_til(data, {"index": tekst_fra_heltall(i), "payload": "norscode-process-metrics"})
        i = i + 1
    }
    assert(lengde(data) == 2000)
    returner 0
}
