// Negativ mellomrekning + mask/borrow (x25519 pack25519-stil).
funksjon start() -> heiltall {
    la t = 100
    la m = t - 65535
    la lo = m & 65535
    la borrow = (m >> 16) & 1
    returner (lo + borrow) & 255
}
