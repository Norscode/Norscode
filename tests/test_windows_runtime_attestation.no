funksjon start() -> heiltall {
    la res = builtin.exec_prosess("NORSCODE_ROOT='.' ./bin/nc run tools/windows_runtime_attestation.no")
    hvis builtin.contains(res[1], "krev faktisk Windows-host") {
        assert_eq(res[0], "2")
    } ellers {
        assert_eq(res[0], "0")
        assert_inneholder(res[1], "Ekte Windows runtime-attestasjon")
    }
    skriv("test_windows_runtime_attestation OK")
    returner 0
}
