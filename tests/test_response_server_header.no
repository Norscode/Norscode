bruk selfhost.nc_main som nc

funksjon start() -> heiltall {
    la resp = nc.response_to_http({"status": 200, "server": "Norscode", "body": "hei"})
    assert_eq(builtin.starts_with(resp, "HTTP/1.1 200"), sann)
    assert_eq(builtin.contains(resp, "Content-Length: 3"), sann)
    returner 0
}
