bruk std.path som p

funksjon start() -> heiltall {
    la sti = p.join("mappe", "fil.no")
    la namn = p.basename(sti)

    hvis namn == "" {
        skriv("std/path.no test FEIL\n")
        returner 1
    }

    skriv("std/path.no test OK\n")
    returner 0
}
