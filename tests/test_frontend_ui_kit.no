bruk std.tekst som t

funksjon krev(src: tekst, needle: tekst) -> heiltall {
    assert(t.inneholder(src, needle))
    returner 0
}

funksjon start() -> heiltall {
    la frontend = fil_les("std/frontend.no")
    krev(frontend, "funksjon ol(")
    krev(frontend, "funksjon details(")
    krev(frontend, "funksjon summary(")
    krev(frontend, "funksjon komponent_knapp(")
    krev(frontend, "funksjon komponent_knapp_sekundær(")
    krev(frontend, "funksjon komponent_lenkeknapp(")
    krev(frontend, "funksjon tooltip_attrs(")
    krev(frontend, "funksjon komponent_ikonknapp(")
    krev(frontend, "funksjon komponent_ikonlenke(")
    krev(frontend, "funksjon komponent_verktøylinje(")
    krev(frontend, "funksjon komponent_tabellhandlinger(")
    krev(frontend, "funksjon komponent_tabell_lenke(")
    krev(frontend, "funksjon komponent_tabell_knapp(")
    krev(frontend, "funksjon komponent_kommandohint(")
    krev(frontend, "funksjon komponent_kommando(")
    krev(frontend, "funksjon komponent_handlingsmeny(")
    krev(frontend, "funksjon komponent_meny_lenke(")
    krev(frontend, "funksjon komponent_meny_knapp(")
    krev(frontend, "funksjon komponent_meny_skille(")
    krev(frontend, "funksjon komponent_breadcrumb(")
    krev(frontend, "funksjon komponent_breadcrumb_lenke(")
    krev(frontend, "funksjon komponent_breadcrumb_aktiv(")
    krev(frontend, "funksjon komponent_filterbar(")
    krev(frontend, "funksjon komponent_bulkbar(")
    krev(frontend, "funksjon komponent_bulkbar_status(")
    krev(frontend, "funksjon komponent_tetthet(")
    krev(frontend, "funksjon komponent_kompakt(")
    krev(frontend, "funksjon komponent_tabs(")
    krev(frontend, "funksjon komponent_tab(")
    krev(frontend, "funksjon komponent_segmentert(")
    krev(frontend, "funksjon komponent_segment(")
    krev(frontend, "funksjon komponent_badge(")
    krev(frontend, "funksjon komponent_status_badge(")
    krev(frontend, "funksjon komponent_avatar(")
    krev(frontend, "funksjon komponent_brukerchip(")
    krev(frontend, "funksjon komponent_brukerlinje(")
    krev(frontend, "funksjon komponent_varsling(")
    krev(frontend, "funksjon komponent_skjelett(")
    krev(frontend, "funksjon komponent_skjelett_linje(")
    krev(frontend, "funksjon komponent_skjelett_kort(")
    krev(frontend, "funksjon komponent_tilstand(")
    krev(frontend, "funksjon komponent_tilstand_handling(")
    krev(frontend, "funksjon komponent_sidehode(")
    krev(frontend, "funksjon komponent_stat(")
    krev(frontend, "funksjon komponent_dashboard(")
    krev(frontend, "funksjon komponent_oppsummering(")
    krev(frontend, "funksjon komponent_oppsummering_rad(")
    krev(frontend, "funksjon komponent_statusboard(")
    krev(frontend, "funksjon komponent_statuskolonne(")
    krev(frontend, "funksjon komponent_statuskort(")
    krev(frontend, "funksjon komponent_listepanel(")
    krev(frontend, "funksjon komponent_listerad(")
    krev(frontend, "funksjon komponent_listerad_statisk(")
    krev(frontend, "funksjon komponent_fremdrift(")
    krev(frontend, "funksjon komponent_stegliste(")
    krev(frontend, "funksjon komponent_steg(")
    krev(frontend, "funksjon komponent_sidebar_shell(")
    krev(frontend, "funksjon komponent_detail_shell(")
    krev(frontend, "funksjon komponent_splitview(")
    krev(frontend, "funksjon komponent_splitview_liste(")
    krev(frontend, "funksjon komponent_splitview_detalj(")
    krev(frontend, "funksjon komponent_panel(")
    krev(frontend, "funksjon komponent_tomt_panel(")
    krev(frontend, "funksjon komponent_disclosure(")
    krev(frontend, "funksjon komponent_disclosure_liste(")
    krev(frontend, "funksjon komponent_meta_liste(")
    krev(frontend, "funksjon komponent_meta_rad(")
    krev(frontend, "funksjon komponent_meta_html(")
    krev(frontend, "funksjon komponent_filterfelt(")
    krev(frontend, "funksjon komponent_felt(")
    krev(frontend, "funksjon komponent_tekstfelt(")
    krev(frontend, "funksjon komponent_felt_validert(")
    krev(frontend, "funksjon komponent_tekstfelt_validert(")
    krev(frontend, "funksjon komponent_felt_hjelp(")
    krev(frontend, "funksjon komponent_felt_feil(")
    krev(frontend, "funksjon komponent_feilsammendrag(")
    krev(frontend, "funksjon komponent_feilpunkt(")
    krev(frontend, "funksjon komponent_valg(")
    krev(frontend, "funksjon komponent_valg_option(")
    krev(frontend, "funksjon komponent_statusvelger(")
    krev(frontend, "funksjon komponent_prioritetvelger(")
    krev(frontend, "funksjon komponent_sortering(")
    krev(frontend, "funksjon komponent_checkbox_rad(")
    krev(frontend, "funksjon komponent_form_actions(")
    krev(frontend, "funksjon komponent_datatabell(")
    krev(frontend, "funksjon komponent_tom_tabell(")
    krev(frontend, "funksjon komponent_paginering(")
    krev(frontend, "funksjon komponent_sideknapp(")
    krev(frontend, "funksjon komponent_ticket_rad(")
    krev(frontend, "funksjon komponent_ticket_rad_med_handlinger(")
    krev(frontend, "funksjon komponent_aktivitet_liste(")
    krev(frontend, "funksjon komponent_aktivitet(")
    krev(frontend, "funksjon komponent_kommentar_boks(")
    krev(frontend, "funksjon komponent_modal(")
    krev(frontend, "funksjon komponent_skuff(")
    krev(frontend, "funksjon komponent_toast_region(")
    krev(frontend, "funksjon komponent_toast_knapp(")
    krev(frontend, "funksjon modal_open_attrs(")
    krev(frontend, "funksjon modal_close_attrs(")
    krev(frontend, "funksjon copy_attrs(")
    krev(frontend, "funksjon livefilter_attrs(")
    krev(frontend, "funksjon toast_attrs(")
    krev(frontend, "funksjon script_toast(")
    krev(frontend, "funksjon ui_script(")
    krev(frontend, "funksjon ui_stilark(")
    krev(frontend, "data_handling(\"modal\", modal_id)")
    krev(frontend, "data_handling(\"dropdown-toggle\", \"#\" + menu_id)")
    krev(frontend, "data_handling(\"tooltip\", tekstverdi)")
    krev(frontend, ".command-hints")
    krev(frontend, ".command-hint")
    krev(frontend, ".command-hint__key")
    krev(frontend, ".action-menu")
    krev(frontend, ".breadcrumb")
    krev(frontend, ".notice")
    krev(frontend, ".skeleton-block")
    krev(frontend, ".skeleton-card")
    krev(frontend, ".skeleton-line")
    krev(frontend, "nc-skeleton-shimmer")
    krev(frontend, ".state-panel")
    krev(frontend, ".state-panel__action")
    krev(frontend, ".progress-block")
    krev(frontend, ".stepper")
    krev(frontend, ".step-current")
    krev(frontend, ".filterbar")
    krev(frontend, ".bulkbar")
    krev(frontend, ".bulkbar__status")
    krev(frontend, ".density")
    krev(frontend, ".density-compact")
    krev(frontend, ".density-compact .data-table th")
    krev(frontend, ".tabs")
    krev(frontend, ".segmented")
    krev(frontend, ".avatar")
    krev(frontend, ".user-chip")
    krev(frontend, ".user-line")
    krev(frontend, ".user-line__text")
    krev(frontend, ".dashboard-grid")
    krev(frontend, ".summary-box")
    krev(frontend, ".summary-row")
    krev(frontend, ".status-board")
    krev(frontend, ".status-column")
    krev(frontend, ".status-card")
    krev(frontend, ".list-panel")
    krev(frontend, ".list-row")
    krev(frontend, ".list-row__aside")
    krev(frontend, ".sidebar-shell")
    krev(frontend, ".detail-shell")
    krev(frontend, ".split-view")
    krev(frontend, ".split-view__list")
    krev(frontend, ".split-view__detail")
    krev(frontend, ".split-view__panel")
    krev(frontend, ".disclosure-list")
    krev(frontend, ".disclosure")
    krev(frontend, ".meta-list")
    krev(frontend, ".activity-list")
    krev(frontend, ".comment-box")
    krev(frontend, ".form-grid")
    krev(frontend, ".form-field")
    krev(frontend, ".field-help")
    krev(frontend, ".field-error")
    krev(frontend, ".error-summary")
    krev(frontend, ".data-table")
    krev(frontend, ".data-table__actions")
    krev(frontend, ".table-actions")
    krev(frontend, ".table-action")
    krev(frontend, ".pagination")
    krev(frontend, ".modal")
    krev(frontend, ".drawer")
    krev(frontend, ".drawer__panel")
    krev(frontend, ".toast")
    krev(frontend, ".tooltip-host")
    krev(frontend, "[data-nc-tooltip]")

    la eksempel = fil_les("examples/frontend.no")
    krev(eksempel, "frontend.ui_stilark()")
    krev(eksempel, "frontend.ui_script()")
    krev(eksempel, "frontend.komponent_breadcrumb(")
    krev(eksempel, "frontend.komponent_breadcrumb_lenke(")
    krev(eksempel, "frontend.komponent_breadcrumb_aktiv(")
    krev(eksempel, "frontend.komponent_varsling(")
    krev(eksempel, "frontend.komponent_skjelett(")
    krev(eksempel, "frontend.komponent_skjelett_linje(")
    krev(eksempel, "frontend.komponent_skjelett_kort(")
    krev(eksempel, "frontend.komponent_tilstand(")
    krev(eksempel, "frontend.komponent_tilstand_handling(")
    krev(eksempel, "frontend.komponent_sidehode(")
    krev(eksempel, "frontend.komponent_kommandohint(")
    krev(eksempel, "frontend.komponent_kommando(")
    krev(eksempel, "frontend.komponent_brukerchip(")
    krev(eksempel, "frontend.komponent_brukerlinje(")
    krev(eksempel, "frontend.komponent_handlingsmeny(")
    krev(eksempel, "frontend.komponent_meny_lenke(")
    krev(eksempel, "frontend.komponent_meny_knapp(")
    krev(eksempel, "frontend.komponent_meny_skille(")
    krev(eksempel, "frontend.komponent_dashboard(")
    krev(eksempel, "frontend.komponent_oppsummering(")
    krev(eksempel, "frontend.komponent_oppsummering_rad(")
    krev(eksempel, "frontend.komponent_statusboard(")
    krev(eksempel, "frontend.komponent_statuskolonne(")
    krev(eksempel, "frontend.komponent_statuskort(")
    krev(eksempel, "frontend.komponent_listepanel(")
    krev(eksempel, "frontend.komponent_listerad(")
    krev(eksempel, "frontend.komponent_listerad_statisk(")
    krev(eksempel, "frontend.komponent_fremdrift(")
    krev(eksempel, "frontend.komponent_stegliste(")
    krev(eksempel, "frontend.komponent_steg(")
    krev(eksempel, "frontend.komponent_tabs(")
    krev(eksempel, "frontend.komponent_tab(")
    krev(eksempel, "frontend.komponent_filterbar(")
    krev(eksempel, "frontend.komponent_bulkbar(")
    krev(eksempel, "frontend.komponent_bulkbar_status(")
    krev(eksempel, "frontend.komponent_kompakt(")
    krev(eksempel, "frontend.komponent_sortering(")
    krev(eksempel, "frontend.komponent_segmentert(")
    krev(eksempel, "frontend.komponent_segment(")
    krev(eksempel, "frontend.komponent_splitview(")
    krev(eksempel, "frontend.komponent_splitview_liste(")
    krev(eksempel, "frontend.komponent_splitview_detalj(")
    krev(eksempel, "frontend.komponent_detail_shell(")
    krev(eksempel, "frontend.komponent_panel(")
    krev(eksempel, "frontend.komponent_tomt_panel(")
    krev(eksempel, "frontend.komponent_disclosure(")
    krev(eksempel, "frontend.komponent_disclosure_liste(")
    krev(eksempel, "frontend.komponent_meta_liste(")
    krev(eksempel, "frontend.komponent_meta_rad(")
    krev(eksempel, "frontend.komponent_meta_html(")
    krev(eksempel, "frontend.komponent_aktivitet_liste(")
    krev(eksempel, "frontend.komponent_aktivitet(")
    krev(eksempel, "frontend.komponent_kommentar_boks(")
    krev(eksempel, "frontend.komponent_filterfelt(")
    krev(eksempel, "frontend.komponent_felt(")
    krev(eksempel, "frontend.komponent_tekstfelt(")
    krev(eksempel, "frontend.komponent_felt_validert(")
    krev(eksempel, "frontend.komponent_tekstfelt_validert(")
    krev(eksempel, "frontend.komponent_feilsammendrag(")
    krev(eksempel, "frontend.komponent_feilpunkt(")
    krev(eksempel, "frontend.komponent_statusvelger(")
    krev(eksempel, "frontend.komponent_prioritetvelger(")
    krev(eksempel, "frontend.komponent_checkbox_rad(")
    krev(eksempel, "frontend.komponent_form_actions(")
    krev(eksempel, "frontend.komponent_datatabell(")
    krev(eksempel, "frontend.komponent_ticket_rad(")
    krev(eksempel, "frontend.komponent_ticket_rad_med_handlinger(")
    krev(eksempel, "frontend.komponent_tabellhandlinger(")
    krev(eksempel, "frontend.komponent_tabell_lenke(")
    krev(eksempel, "frontend.komponent_tabell_knapp(")
    krev(eksempel, "frontend.komponent_tom_tabell(")
    krev(eksempel, "frontend.komponent_paginering(")
    krev(eksempel, "frontend.komponent_sideknapp(")
    krev(eksempel, "frontend.komponent_modal(")
    krev(eksempel, "frontend.komponent_skuff(")
    krev(eksempel, "frontend.komponent_toast_region(")
    krev(eksempel, "frontend.komponent_toast_knapp(")
    krev(eksempel, "frontend.komponent_ikonknapp(")
    krev(eksempel, "frontend.komponent_ikonlenke(")
    krev(eksempel, "frontend.modal_open_attrs(")
    krev(eksempel, "frontend.copy_attrs(")
    krev(eksempel, "frontend.toast_attrs(")

    la docs = fil_les("docs/frontend/FRONTEND_MODEL.md")
    krev(docs, "## UI-kit")
    krev(docs, "sidehode med handlingar")
    krev(docs, "brødsmulesti og varsel")
    krev(docs, "skjelettvisning for innlasting")
    krev(docs, "tilstandspanel med handlingar")
    krev(docs, "ikonknappar og tooltip")
    krev(docs, "handlingsmeny og menyelement")
    krev(docs, "kommandohint og tastaturmerker")
    krev(docs, "avatar, brukerchip og brukerlinje")
    krev(docs, "tabs, segmentert kontroll og filterbar")
    krev(docs, "bulkbar for massehandlingar")
    krev(docs, "tetthetswrapper og kompakt visning")
    krev(docs, "tabellhandlingar for kompakte radknappar")
    krev(docs, "datatabell")
    krev(docs, "status- og prioritetvelger")
    krev(docs, "sorteringsvelger")
    krev(docs, "oppsummeringsboks og nøkkelrader")
    krev(docs, "statusboard med kolonnar og sakskort")
    krev(docs, "listepanel og kompakte listerader")
    krev(docs, "fremdrift/SLA-visning")
    krev(docs, "stegindikator for saksgang")
    krev(docs, "ticket-rad for helpdesk-lister")
    krev(docs, "split-view for liste og detalj")
    krev(docs, "detalj-layout, metadata og aktivitetslogg")
    krev(docs, "sammenleggbare disclosure-seksjonar")
    krev(docs, "kommentarboks og tomt panel")
    krev(docs, "side-skuff for hurtigvisning")
    krev(docs, "validerte felt, feltfeil og feilsammendrag")
    krev(docs, "frontend.ui_stilark()")
    krev(docs, "frontend.ui_script()")
    returner 0
}
