bruk std.tekst som tekstmod

funksjon lengde(v) -> heiltall { returner 999 }

funksjon start() -> heiltall {
    assert_eq(lengde("abc"), 999)
    assert_eq(builtin.lengde("abc"), 3)
    assert_eq(builtin.lengde([1, 2, 3, 4]), 4)
    assert_eq(tekstmod.lengde("norscode"), 8)
    returner 0
}
