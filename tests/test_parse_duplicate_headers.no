bruk selfhost.nc_main som nc

funksjon start() -> heiltall {
    la resp = nc.response_to_http({"status": 200, "body": "ok"})
    assert_eq(builtin.contains(resp, "Content-Length: 2"), sann)
    returner 0
}
