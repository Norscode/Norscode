bruk std.tekst som t

funksjon start() -> heiltall {
    la src = fil_les("std/deque.no")

    assert(t.inneholder(src, "reserver plass bakerst før verdiane flyttast fram"))
    assert(ikkje t.inneholder(src, "dummy bakerst"))

    skriv("test_deque_contract_text OK")
    returner 0
}
