bruk selfhost.nc_main som nc

funksjon start() -> heiltall {
    la resp = nc.response_to_http({"status": 200, "body": "reload"})
    assert_eq(builtin.starts_with(resp, "HTTP/1.1 200"), sann)
    assert_eq(builtin.contains(resp, "Content-Length: 6"), sann)
    returner 0
}
