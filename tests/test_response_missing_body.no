bruk selfhost.nc_main som nc

funksjon start() -> heiltall {
    la resp = nc.response_to_http({"status": 200})
    assert_eq(builtin.contains(resp, "Content-Length: 0"), sann)
    assert_eq(builtin.ends_with(resp, "\r\n"), sann)
    returner 0
}
