bruk selfhost.nc_main som nc

funksjon start() -> heiltall {
    la resp = nc.response_to_http({"status": 500, "body": "oops"})
    assert_eq(builtin.starts_with(resp, "HTTP/1.1 500"), sann)
    assert_eq(builtin.contains(resp, "Content-Length: 4"), sann)
    returner 0
}
