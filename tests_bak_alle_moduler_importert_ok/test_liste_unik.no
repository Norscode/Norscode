bruk selfhost.common som sh

funksjon start() -> heltall {
    la a: liste_tekst = sh.ny_tekst_liste()
    legg_til(a, "one")
    la b: liste_tekst = sh.ny_tekst_liste()
    legg_til(b, "two")
    skriv(tekst_fra_heltall(lengde(a)))
    skriv(tekst_fra_heltall(lengde(b)))
    returner 0
}
