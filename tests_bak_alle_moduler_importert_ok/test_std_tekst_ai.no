bruk std.tekst som t

funksjon start() -> heiltall {
    la verdi = "Norscode tekst test"
    la pos = t.finn(verdi, "tekst")

    hvis pos < 0 {
        skriv("std/tekst.no test FEIL\n")
        returner 1
    }

    skriv("std/tekst.no test OK\n")
    returner 0
}
