bruk selfhost.nc_main som nc

funksjon start() -> heiltall {
    la resp = nc.response_to_http({"status": 204, "body": ""})
    assert_eq(builtin.starts_with(resp, "HTTP/1.1 204"), sann)
    returner 0
}
