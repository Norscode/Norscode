# Generert C (ikkje i git)

`norscode_generated.c` og `nc_dispatch.c` vert laga av `tools/regen_native.sh` frå `.no`-kjelda.

```bash
bash tools/build_norscode_native.sh   # seed → regen → clang
# eller
bash tools/regen_native.sh --rebuild
```
