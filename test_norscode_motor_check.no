bruk norscode_ai_motor som motor

funksjon start() -> heiltall {
    la queue = "{\"items\": []}\n"
    hvis motor.motor_queue_har_item(queue, "queue-010") {
        skriv("har")
    } ellers {
        skriv("ikkje")
    }
    returner 0
}
