// package-release.no — direkte alias for Norscode-native releasepakking
// bruk: nc package-release <versjon>

bruk tools.package_release som package_release

funksjon start() -> heiltall {
    returner package_release.start()
}
